"""Projects 7.14 — ClientExternalCollaboration models sub-package (one module per entity).

Intentionally EMPTY of re-exports by convention: the package's public surface is the top-level
``apps/projects/models/__init__.py`` re-export block. Every sub-package in this app keeps its own
``__init__.py`` free of imports, which is what makes the "missing re-export is an ImportError" rule
visible.
"""
