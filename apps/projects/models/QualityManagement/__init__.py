"""Projects 7.6 — QualityManagement models sub-package (one module per entity).

Intentionally EMPTY: the package's public surface is the top-level
``apps/projects/models/__init__.py`` re-export block, added in the Integrate step. Keeping this
file empty until then is what makes the "missing re-export is an ImportError" rule visible.
"""
