"""Projects 7.7 — ScopeRequirements forms sub-package (one module per entity).

This package holds the per-entity modules; their public surface is re-exported from the
top-level ``apps/projects/forms/__init__.py``. Keep that re-export block in step with the
modules added here.
"""
