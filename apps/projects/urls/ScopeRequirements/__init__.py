"""Projects 7.7 — ScopeRequirements urls sub-package (one module per entity).

This package holds the per-entity modules; ``urlpatterns`` are concatenated into the
top-level ``apps/projects/urls/__init__.py``. Keep that concatenation in step with the
modules added here — an un-concatenated module's routes simply do not exist.
"""
