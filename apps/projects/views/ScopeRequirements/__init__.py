"""Projects 7.7 — ScopeRequirements views sub-package (one module per entity).

This package holds the per-entity modules; their public surface is re-exported from the
top-level ``apps/projects/views/__init__.py``, which is what the URLconf resolves against.
Keep that re-export block in step with the modules added here.
"""
