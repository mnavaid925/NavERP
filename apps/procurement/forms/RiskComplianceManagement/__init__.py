"""Procurement 6.17 Risk & Compliance Management — sub-package init (docstring-only).

Re-exports live in ``apps/procurement/forms/__init__.py`` — the app-level package is the single
re-export point (6.13/6.14/6.15 precedent). Entity modules: ``Screenings`` (the screening
create/edit form, the hit capture form, and the plain disposition form the adjudication verb
validates its POST with).
"""
