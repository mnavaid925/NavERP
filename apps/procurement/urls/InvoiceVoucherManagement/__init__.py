"""6.13 Invoice & Voucher Management urlconf modules.

Deliberately NO re-export block here — ``urls/__init__.py`` imports each entity module's
``urlpatterns`` directly and splats them LAST (see the ordering comment there).
"""
