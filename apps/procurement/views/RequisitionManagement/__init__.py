"""Procurement 6.2 Requisition Management views sub-package.

The views are re-exported by the app-level ``apps.procurement.views`` package (that is what the
URLconf's ``views.<name>`` resolves against), so this __init__ stays a marker.
"""
