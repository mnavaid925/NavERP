"""Procurement 6.15 Budget & Cost Management — sub-package init (docstring-only).

Re-exports live in ``apps/procurement/models/__init__.py`` — the app-level package is the single
re-export point (6.13/6.14 precedent). Entity modules: ``BudgetMappings`` (the budget-mapping
configuration master + the commitment-status vocabulary) and ``CostForecasts`` (the frozen
forecast snapshot + its compute function).
"""
