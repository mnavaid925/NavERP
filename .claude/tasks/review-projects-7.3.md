# Review — Projects 7.3 Resource Management

Changeset: `030fb0aa..HEAD` (7.3 files/hunks only; 7.4's interleaved commits are out of this lane's scope). Contract: `.claude/tasks/contract-projects-7.3.md`.

**Phase 4 of the CLAUDE.md Module Creation Sequence.** Six reviewers ran one after another
(code-reviewer → explorer → frontend-reviewer → performance-reviewer → qa-smoke-tester →
security-reviewer), each appending its section below. A CONCURRENT 7.4 session shares this
checkout — its commits are excluded from every lane's scope. IDs below are assigned after the
dedupe. **Phase 5 (`code-fixer`) works the consolidated triage, in ID order; the per-reviewer
sections are the evidence, not the worklist.**

---

# Consolidated triage — THE authoritative fix list

~32 raw findings across six reviewers, deduped to **18** (0 Critical / 7 Important / 11 Minor)
plus 5 recorded no-actions. **`code-fixer` works this list, in this order.** Tick
`[x] fixed` / `[~] skipped — reason` as you go; nothing may be left `[ ] open`. Verify each fix
(`manage.py check` + re-run the relevant page/verb), and commit **one file per commit**
(PowerShell-safe, no `git push`). Two of the items are doc-only (M10, M11). Routing at the
bottom: the test suite is Phase 6's, README/SKILL.md are Phase 7's.

## Critical

None across all six lanes.

## Important

- [x] fixed **I1 — Approve-Week `confirm()` interpolates a user-writable name (L42; lanes 1-I1, 3-I1, 6-xref).** `templates/projects/resource/resourcetimeentry/list.html:67` puts `{{ group.list.0.resource.name }}` (→ `Party.name`, member-writable) inside the inline JS string; the attribute decode restores a bare `'` before JS compiles, so `O'Brien` breaks the handler and the bulk-approve POST submits **with no confirmation** (and it is the changeset's one stored-XSS shape — script would run in an admin's session). Fix: interpolate `{{ group.list.0.resource.number }}` (system token, like every other confirm in the changeset); the name may stay in the button label (HTML context, autoescaped). — 9430896f
- [x] fixed **I2 — Assign/Substitute `{% if %}` precedence bugs render buttons the verbs always refuse (lanes 3-I2, 5-M3 runtime-confirmed).** `resourceallocation/detail.html:18` `not obj.resource_id and obj.booking_status == 'requested' or obj.booking_status == 'soft'` parses as `(not resource_id and requested) or soft` → Assign renders on named soft bookings; `:30` renders Substitute on firm placeholders. Django `{% if %}` has no parentheses — nest two tags: status gate outer, `{% if not obj.resource_id %}` (resp. `{% if obj.resource_id %}`) inner. Amend the contract's pinned condition strings (Template section + divergence list) when fixing. — 4084798d + contract d8de9d30
- [x] fixed **I3 — a placeholder can be committed to `firm`: an unstaffable dead end invisible on the board (lane 1-I2).** `ral_commit` (`ResourceAllocations.py:197-211`) has no resource guard; a firm placeholder appears in NEITHER capacity (`resource_id__isnull=False`) NOR demand (`requested/soft`), and assign refuses firm / substitute refuses placeholders — the hiring-trigger demand silently vanishes; only cancel recovers. Fix: in `ral_commit`, refuse soft→firm while `resource_id is None` with `messages.error("Assign a resource before committing a placeholder to firm.")` + redirect (requested→soft stays legal — that IS the pipeline signal). — 7e9be3a0
- [x] fixed **I4 — the rsp_detail "Book" deep-link passes `?resource=`, which `ral_create` never reads (lanes 1-I3, 3-I4).** `ResourceAllocations.py:79-80` builds `initial={"project": …}` only, so Book opens the form with an empty Resource select. Fix: `initial={"project": request.GET.get("project", ""), "resource": request.GET.get("resource", "")}` (mirrors `rte_create`). — c661e34d
- [x] fixed **I5 — the person-week subheader is unreadable in dark mode: `--surface-muted` doesn't exist (lane 3-I3).** `resourcetimeentry/list.html:63` `style="background:var(--surface-muted, #f8fafc);"` — the var is defined nowhere, so the near-white fallback applies in both modes (light-on-light under `html.dark`), and the inline background outranks the tbody hover the stylesheet comments about. Fix: `style="background:var(--page-bg);"`. — ffbc14e3
- [x] fixed **I6 — the allocation register renders `obj.resource.name` through an unselected two-hop walk: up to +30 queries per 15-row page (lane 4-I1).** Fix: `ResourceAllocations.py:35` → `select_related("project", "project_request", "project_task", "resource__employee__party", "resource__party")`. — 698cd8c0
- [x] fixed **I7 — every resource dropdown/form label walks the same two hops per pool row (lane 4-I2).** Fix: `views/_helpers.py` `resource_profiles()` returns the queryset with `.select_related("employee__party", "party")`; and in the two 7.3 forms (`ResourceAllocationForm`, `ResourceTimeEntryForm`) re-set the resource field's queryset in `__init__` (after `super().__init__`): `self.fields["resource"].queryset = self.fields["resource"].queryset.select_related("employee__party", "party")` — per-form line, NOT an app-wide `TenantModelForm` change. — 6130523a + 91d1cee4 + 8ff651d9

## Minor

- [x] fixed **M1 — the RSP delete-confirm copy overstates the blast radius (lanes 1-M1, 3-M2).** `resourceprofile/list.html:74` + `detail.html:75` say "Its bookings and time entries are deleted with it", but `ResourceAllocation.resource` is SET_NULL — bookings survive as placeholders. Fix the copy in both files: "Its time entries are deleted with it; its bookings stay on as placeholders." — c9c67d58 + d1ee0069
- [x] fixed **M2 — Edit/Delete offered on locked rows (lanes 1-M2, 3-M1).** `resourcetimeentry/detail.html:28` (Edit) + `:62-67` (Delete) render unconditionally on approved/rejected rows where the views refuse; same for the register row pencil/trash (`list.html:94-97`). Wrap each in `{% if obj.status == 'draft' or obj.status == 'submitted' %}` (7.2 frozen-row idiom). — 8c949f8a + 51213a7b
- [x] fixed **M3 — the actuals card's Planned column renders the unquantized zero sentinel (lane 5-M1).** `ResourceTimeEntries.py:69` — quantize the row-built `planned_hours` (house `q2`) so `0h` never sits next to `2.00h`. — 31635081
- [x] fixed **M4 — the over-allocation alert branch is seed-dead (lane 5-M2).** No seeded cell ever exceeds capacity, so the red headline never fires for a user. Fix in the seeder: raise RAL-00001 to `48h/wk` (RSP-00001's 40h capacity) so one cell goes over; keep every other number honest. NOTE: reseed after (`seed_projects --flush`) and re-verify the board shows the red alert + green all-clear states. — a20df48c; reseeded both tenants, board shows over_count=4 (W37–W40 at 48h vs 40h; W41 partial 27.43h stays under), red alert renders; qa73_review.py re-run 108/108 green (its 4 over_count/16h expectations updated in the temp script only — the contract pins row shapes, not the 16h figure)
- [x] fixed **M5 — `ral_substitute`'s status guard races outside the atomic block (lane 6-M1).** Two concurrent admin POSTs can release one booking and mint two successors. Fix per the `prq_convert` precedent: inside `transaction.atomic()`, re-fetch `ResourceAllocation.objects.select_for_update().get(pk=obj.pk, tenant=request.tenant)` and re-run the status check (and refuse when `substituted_by.exists()`). — 16eb7e70
- [x] fixed **M6 — the capacity horizon select is JS-only (lane 3-M3).** `capacity_demand.html` relies on `onchange` submit with no button. Add a `<noscript><button class="btn btn-outline" type="submit">Apply</button></noscript>` inside the form. — c1000193
- [x] fixed **M7 — same one-token select_related extensions at the single-row sites (lane 4-M1).** `ral_detail` → add `"resource__employee__party", "resource__party", "requested_by"` to its tuple; `rte_detail` → `"resource__employee__party", "resource__party"`; `_actuals_rows`' approved queryset → `"resource__employee__party", "resource__party"`. — 175922b6 + 5e9d0f99
- [x] fixed **M8 — %-unit bookings pay +1 query each in both computed pages (lane 4-M2 + lane 1 routing).** Add `"resource"` to `capacity_allocs` (`CapacityDemand.py:54-58`) and `_actuals_rows`' `live_allocs` (`ResourceTimeEntries.py:49-53`). — fd98eebc + 3aebfa0b
- [x] fixed **M9 — `rte_approve_week` writes N UPDATEs where one bulk fits (lane 4-M5).** Stamp `updated_at` manually and `bulk_update(entries, ["status", "approved_by", "approved_at", "updated_at"])` inside the same atomic (bulk_update skips auto_now). — 63c166f3
- [x] fixed **M10 — `_helpers.py:3-6` module docstring is stale after the 7.3 additions (lane 2 Low).** It still enumerates "the first three … the fourth"; update to describe all six helpers (the 7.2 fixer fixed the same staleness for the fourth in `b2c0e06d`). — 235bf9e2
- [x] fixed **M11 — amend the contract to as-built, two safe-direction drifts (lane 1-M3/M4).** `.claude/tasks/contract-projects-7.3.md`: (a) divergence note 5 — `?year=0`/`?year=9999` now return the default page via `qs.none()` + actuals-window fallback (Django's year-lookup bounds otherwise raise inside COUNT) instead of "legitimately match no rows"; (b) wire-up step 7 — admin `readonly_fields` additionally freeze `booking_status`/`substitute_of`/`requested_by` (RAL) and `status`/`decision_note` (RTE) so Django admin cannot bypass the audited verbs. Contract amendments only — do NOT revert the code. — 36c354c4

## Recorded, no action — do NOT "fix" these

- Approve Week renders on person-week groups with zero submitted entries — the POST bounces with the pinned info refusal; the contract pins the button unconditionally and a template cannot express "any submitted in group" without a custom filter (lane 3-M4).
- `?year=`/`?week=` compile to function-wrapped SQL no b-tree serves, and the actuals window uses only index prefixes — tenant-bounded, paginated, acceptable (lane 4-M3).
- RTE `Meta.ordering` mixed direction filesorts tenant rows on unfiltered renders — the regroup correctness holds; acceptable (lane 4-M4).
- `NavERP-ERD.md` module-7 row never named 7.1/7.2's entities either — pre-existing maintenance pattern (lane 2 Low).
- `project_requests()` has one consumer against `_helpers`' placement rule — kept next to `resource_profiles()` as the demand-lens pair, documented (lane 2 Low).

## Routing (not fixer items)

- **Phase 6:** the `test_resource_*` suite does not exist yet — `conftest.py` needs `resource_*` fixtures; lane 1's "Suggested tests" and lane 4's `django_assert_max_num_queries` routing notes are the seed list.
- **Phase 7:** README.md roadmap row still says "7.1–7.2 built — 2 of 19" (lane 2 Medium); `.claude/skills/projects/SKILL.md` still says "As-built: 7.1 only" — both are the docs pass.

---

## 1. code-reviewer

Verdict: Contract-faithful build with strong tenancy/verb/audit hygiene and no Critical findings — commit after fixing the three Important items (user name inside a confirm() string, the firm-placeholder dead end, the dead Book deep-link param).

### Critical

None. Tenancy holds everywhere: every queryset and every `get_object_or_404` in all four view modules carries `tenant=request.tenant` (incl. all 9 verbs and both computed sections), forms re-check FKs via `_reject_foreign` on exactly the contract's lists (User FKs excluded per contract), and the seeder writes only `tenant=tenant` rows.

### Important

- **`templates/projects/resource/resourcetimeentry/list.html:67 — a user-writable name is interpolated into the Approve-Week `confirm()` string (L42; the 7.2-I3 class).** `confirm('Approve every submitted entry for {{ group.list.0.resource.name }} in week …')` — `resource.name` resolves to `Party.name` (user-editable `CharField`). Django's attribute escaping is no protection here: `&#x27;` decodes back to a bare `'` before the JS engine compiles the handler, so an innocent `O'Brien Consulting` throws in the handler and — because a thrown handler returns `undefined` — **the week's entries are bulk-approved with no confirmation at all**; a crafted name is the stored-XSS shape 7.2 already triaged as Important (every other confirm in this changeset correctly interpolates only the system `{{ obj.number }}`). Fix: interpolate `group.list.0.resource.number` (or the week number) and put the name in the button label.
- **`apps/projects/views/ResourceManagement/ResourceAllocations.py:197-211 + templates/projects/resource/resourceallocation/detail.html:43-47 — a placeholder can be committed to `firm`, an unstaffable state that vanishes from the board.** `ral_commit` has no resource guard, so two member-level POSTs take a requested placeholder → soft → firm. A firm placeholder appears in NEITHER capacity section (`resource_id__isnull=False`, CapacityDemand.py:55) NOR demand (`booking_status__in=("requested","soft")`, CapacityDemand.py:77) — and it is a dead end: `ral_assign` refuses firm ("cannot be staffed", :122) and `ral_substitute` refuses placeholders (:148), so the only exit is cancel. The unfilled hiring-trigger demand silently disappears (L39's dead-end shape). Contract-conformant on paper (the verb table pins no resource guard), but it deserves a one-line refusal: soft→firm while `resource_id is None` → "Assign a resource before committing a placeholder to firm." (or count firm placeholders in the demand section).
- **`templates/projects/resource/resourceprofile/detail.html:11 + apps/projects/views/ResourceManagement/ResourceAllocations.py:79-80 — the "Book" deep-link passes `?resource=<pk>`, which `ral_create` never reads.** `ral_create` builds `initial={"project": request.GET.get("project", "")}` only, so clicking Book on a resource's page opens the allocation form with an empty Resource select — the exact dead-param class 7.2 consolidated as I5 (`dep_create?project=`). Fix: read `?resource=` into `initial` too (mirrors `rte_create`'s `?resource`), or drop the param from the link.

### Minor

- **`templates/projects/resource/resourceprofile/list.html:74 + detail.html:75 — the delete confirm misstates the consequence.** "Its bookings and time entries are deleted with it." — `ResourceAllocation.resource` is `SET_NULL` (migration 0004, line 68), so bookings survive as placeholders; only time entries (`CASCADE`) die. Fix the copy ("Its time entries are deleted with it; its bookings stay on as placeholders.").
- **`templates/projects/resource/resourcetimeentry/detail.html:28,62-67 — Edit/Delete render unconditionally on `approved`/`rejected` rows**, where the views always refuse (ResourceTimeEntries.py:159-161, 172-174) — every click ends in the refusal redirect. The sibling idiom (7.2 baselines: Edit/Delete hidden exactly when frozen) wraps them in `{% if obj.status == 'draft' or obj.status == 'submitted' %}`. (View-level guards are correct — this is offer-what-the-view-will-refuse only.)
- **Contract drift, documented and safer (rte_list `?year=0`/`?year=9999`)** — contract divergence note 5 (~line 691) says these "legitimately match no rows"; in reality Django's year-lookup bounds raise inside `.count()` (a 500), fixed in commit `bf67c47d` with `qs.none()` + actuals-window fallback (`ResourceTimeEntries.py:87-105`, `?year=0&week=37` no longer reaches `fromisocalendar(0, …)`). No pinned smoke assert contradicts it — amend note 5 rather than revert.
- **Contract drift, safe direction (admin readonly)** — the contract's wire-up step 7 pins `readonly_fields = ("created_at","updated_at")` (+ the RTE stamps); the build additionally froze `booking_status`/`substitute_of`/`requested_by` (`admin.py:131`) and `status`/`decision_note` (`admin.py:143`) so Django admin cannot bypass the audited verbs — the 7.2-M9 lesson applied. Amend the contract to as-built.

### Verified correct

- **Models vs contract, field-for-field:** all three CHOICES tuples verbatim; `allocation_unit` max_length 14 / `booking_status` 12; every field's null/blank/on_delete/related_name/default/validators match (incl. `editable=False` on `requested_by` + the three RTE stamps); Meta.orderings pinned as specced; `unique_together ("tenant","number")` on all three; all 12 index names byte-match (`rsp_tnt_*`, `ral_tnt_*`, `rte_tnt_*`); `__str__` shapes match (RTE's has no FK deref); migration `0004` creates the three tables + 12 named indexes with `hrm` in its dependencies and no destructive ops; `makemigrations --check` clean at HEAD.
- **Derived + guards:** `ResourceProfile.name`/`clean()` (exactly-one-of, one-row-per-employee per tenant, no conditional unique constraint — 7.2 BSL precedent); `is_live` and its Q-twin `_live_q` share one predicate; `planned_hours` proration matches the pinned spec for all three units, including released→ZERO, null-end clamped to `win_end`, pct-of-placeholder→ZERO, and the total-hours own-window spread with the `window_days <= 0` guard; `q2`/`ZERO` from `_base` used throughout.
- **Forms:** `TenantUniqueMixin` before `TenantModelForm` on all three; field lists and order match the contract exactly; verb-driven/provenance fields (`booking_status`, `substitute_of`, `requested_by`, `status`, stamps, `decision_note`) all excluded; `_reject_foreign` on exactly the pinned FK lists; the pinned help notes render on the forms.
- **Verbs:** all 9 with the contract's gate order (`@login_required` → `@tenant_admin_required` where pinned → `@require_POST`), allowed-from sets, refusal texts byte-matching the table (error vs info variants), audit actions ≤ 10 chars (`assign`/`substitute`/`commit`/`complete`/`cancel`/`submit`/`approve`/`reject`), changes dicts carrying the verb detail, stamps written exactly once via `update_fields`, `ral_substitute` inside `transaction.atomic()` copying the pinned field set + redirect to the successor, `rte_approve_week` clamping `1..53`, one audit row on the resource with the pinned changes dict, and `ral_assign`/`ral_substitute` tenant-resolving the POSTed resource pk via `as_db_int`.
- **Lists/filters:** all three lists route through `crud_list` with the pinned search_fields/filters parsed before pagination; the hand-parsed `?placeholder=`/`?is_live=` lenses ignore other values; `?year`→`entry_date__iso_year` paired with `entry_date__week` (ISO-consistent); `per_page` default 15; context keys match what each template consumes (`resource_type_choices`, `status_choices`, `org_units`, `booking_status_choices`, `projects`, `project_requests`, `resources`, `status_choices`, `actuals_rows` + the window keys the header renders).
- **Computed sections:** `_actuals_rows` ignores `?q=`/`?status=`, keys rows by (resource, project) pairs that actually logged approved time, sums live soft/firm `planned_hours` per pair, orders variance descending; the capacity board sums soft/firm only per active resource-week, `over = planned > weekly_capacity_hours`, `over_count` drives the alert line, demand = requested/soft overlapping the horizon with project-linked first, `demand_label`/`demand_url_name`/`demand_url_pk`/`demand_hours`/`is_gap`/`gap_count` all as pinned, caveat line verbatim, `id="actuals"` + `id="demand"` anchors present.
- **URLs/packages:** all 25 routes byte-match the table; `week/` literal precedes the `<int:pk>/` routes with the commented house rule; literal-first inside every module; the four `_rm_*` imports + concatenation in `urls/__init__.py` after `_pp_baselines` with disjoint first segments; re-export blocks in all four top-level `__init__.py` files verbatim; sub-package `__init__`s empty; absolute imports; `views/_helpers.py` gains `resource_profiles`/`project_requests` in the house shape (`.none()` for tenant None, dropdown ordering).
- **Templates:** every `{% url %}` target resolves (`projects:overview/prj_detail/prq_detail/tsk_detail/ral_*/rsp_*/rte_*`, `hrm:employee_detail`, `hrm:employeeskill_list`, `dashboard:home`); FK dropdowns compare with `|stringformat:"d"`; badge maps colour-named only with `{% else %}`+`get_*_display` fallbacks everywhere (booking/status/type maps exact, `completed`→`badge-info` per divergence 7); `{% comment %}` for the multi-line notes (no `{# #}` leaks); `{% csrf_token %}` on every POST form; no nullable FK inside a `|default:` argument; pagination include present on all three lists; empty states with the pinned icon/copy; admin-gated verb buttons (Assign/Substitute/Approve/Reject/Approve Week) wrapped in the `is_superuser or is_tenant_admin` idiom; detail grids carry every pinned row (Substitute of/Substituted by/Requested by/Decision note).
- **Seeder:** `_resourcing` runs after `_planning` with its own guard and the pinned "already exist. Use --flush to re-seed." wording; per-row `.save()` (no bulk_create); all 10 allocations match the pinned shape (statuses, all three units, both placeholder kinds, offsets, the released→successor chain, first work package on row 2, pct 50 on the 24h part-timer); 16 time entries across resources #1–#3 and the three ISO weeks covering all four statuses with honest stamps (submitted_at/approved_by/approved_at set per status) and the non-project approved row; users/Party guard, and no-active-project/no-request degradation warns and skips only the affected rows; `--flush` extends children-first with RTE → RAL → RSP before the 7.2 deletions and the help string names them.
- **Wire-up:** `LIVE_LINKS["7.3"]` byte-matches the contract block at `apps/core/navigation.py:1731` (five verbatim bullet names + Time Approvals leaf + fragments); Overview adds the three flat counts (`Overview.py:65-67`); `templates/projects/overview.html` adds the three stat cards after Baselines (blue/orange/green — valid palette) and the four quick-link rows; intro copy mentions the 7.3 layer.

### Suggested tests

- `apps/projects/tests/test_resource_views.py` — (a) Approve-Week confirm string renders only system-generated tokens (no `resource.name` interpolation); (b) a member committing a placeholder soft→firm is refused while `resource_id is None`; (c) `ral_create?resource=<pk>` pre-selects the resource; (d) `?year=0`/`?year=9999` on `rte_list` render the default page with no 500; (e) approved/rejected entries refuse edit/delete POSTs with row unchanged (lock rule); (f) substitute releases the original and creates a successor copying the window/magnitude with `substitute_of` set.

### Routing

- performance-reviewer: `planned_hours` on `pct_capacity` bookings dereferences `self.resource.weekly_capacity_hours`, but neither `capacity_allocs` (`CapacityDemand.py:54-58`) nor `_actuals_rows`' `live_allocs` (`ResourceTimeEntries.py:49-53`) `select_related("resource")` — one extra query per %-unit booking per render (cold path, dozens of rows).
- security-reviewer: the Important confirm()-interpolation finding above (`resourcetimeentry/list.html:67`) is the stored-XSS-shaped one to adversarially verify.

---

## 2. explorer

Verdict: The built 7.3 is aligned with the documented spec layer — all five NavERP.md bullets map to live, resolving sidebar pages that realize their promise (with the contract's documented stand-ins for smoothing/availability/header-timesheet), the documented cross-module exclusions hold in the code; the one real gap is that README.md's Module 7 roadmap row was not updated for 7.3.

### Findings

- **README.md roadmap row not updated for 7.3 (Medium — doc consistency).** `README.md:1200` still reads "🟦 7.1–7.2 built — 2 of 19 sub-modules" and narrates only 7.1/7.2; no 7.3 paragraph was added, and the route table has no Module 7 row at all (no `/projects/` entry anywhere in that table — pre-existing). Every built sub-module so far got a README row paragraph (7.1 and 7.2 both do, same line), so 7.3 going live in this changeset leaves the repo's own roadmap understating the module. `git diff --stat BASE..HEAD -- README.md` is empty. (7.4 is in the same boat but is out of scope here.) Fix: a docs-pass commit extending line 1200 to "7.1–7.3 built — 3 of 19" with the RSP/RAL/RTE narrative.
- **NavERP-ERD.md module-7 row does not name the resource entities (Low — pre-existing pattern, documented divergence).** `NavERP-ERD.md:469` "Adds" column is the original catalog snapshot ("ProjectTask, Milestone, Timesheet, RiskItem, ChangeRequest") — it never got 7.1/7.2's entities either, so 7.3's `ResourceProfile`/`ResourceAllocation`/`ResourceTimeEntry` being absent is consistent with how this module's rows have been maintained. The one substantive alignment question — the ERD's "Timesheet" under Module 7 — is realized as a deliberate, documented divergence: no timesheet header model (flat entries + weekly verbs), because `hrm.TimesheetEntry.project` points at the 2.9 `accounting.Project` stand-in and cannot join `projects.Project` (research ruling at `.claude/tasks/research-projects-7.3.md:419` and `:48`; restated in the model docstring `apps/projects/models/ResourceManagement/ResourceTimeEntries.py:4-7`). The ERD "Reuses" column (`Project · Activity · Employment · …`) holds: the pool reuses `hrm.EmployeeProfile` (1:1 over `core.Party`/`Employment`) and bare `core.Party` for externals — it never copies a person table.

---

## 3. frontend-reviewer

Verdict: Contract-faithful, leak-free templates with every context key sourced and every url resolving — no Critical findings, but four Important fixes before commit: the Approve-Week `confirm()` interpolates a user-writable name (L42), the Assign/Substitute `{% if %}` gates have `and`/`or`-precedence bugs that render buttons the verbs always refuse, the person-week subheader is unreadable in dark mode (`--surface-muted` doesn't exist), and the "Book" deep link passes a param the view never reads.

### Critical

None found. No `{#` comment-leak risk anywhere in `templates/projects/resource/` (grep: zero occurrences; the two multi-line notes use `{% comment %}`, `resourceallocation/detail.html:11-16`, `resourceprofile/detail.html:53-56`); no invented theme class; no CSRF-less POST; no nullable FK inside a `|default:` argument.

### Important

- **`templates/projects/resource/resourcetimeentry/list.html:67` — the Approve-Week `confirm()` interpolates a user-writable name into a single-quoted JS string (L42).** `confirm('Approve every submitted entry for {{ group.list.0.resource.name }} in week …')` — `resource.name` walks to `core.Party.name`/the employee's party name, both user-editable. Auto-escaping is no protection in an event-handler attribute (HTML-unescaping restores a bare `'` before the JS compiles), so a resource named `O'Brien` breaks the handler and the browser submits the bulk-approve POST **with no confirmation dialog**. Every other confirm in the sub-module interpolates only the system-generated `{{ obj.number }}` — safe. Fix: interpolate `{{ group.list.0.resource.number }}` (or wrap the name in `|escapejs`).
- **`templates/projects/resource/resourceallocation/detail.html:18` and `:30` — the Assign/Substitute gates have Django `{% if %}` operator-precedence bugs, rendering verb buttons the views always refuse.** `not obj.resource_id and obj.booking_status == 'requested' or obj.booking_status == 'soft'` parses as `(not resource_id and requested) or soft` (`and` binds tighter than `or`), so **Assign renders on every named soft booking** — the normal state after a successful assign — where `ral_assign` always refuses ("already names a resource", `ResourceAllocations.py:118-121`); symmetrically `:30` renders **Substitute on firm placeholders**, refused at `:148-151`. The build matches the contract's pinned condition strings verbatim, but the pinned semantics diverge from the views' real guards (charter: status-gated conditions must match the views) — an always-erroring button is worse than none. Fix: nest two tags — Assign: `{% if obj.booking_status == 'requested' or obj.booking_status == 'soft' %}{% if not obj.resource_id %}…{% endif %}{% endif %}`; Substitute: status-outer, `{% if obj.resource_id %}`-inner (Django `{% if %}` has no parentheses). Amend the contract strings when fixing.
- **`templates/projects/resource/resourcetimeentry/list.html:63` — the person-week subheader is unreadable in dark mode: `--surface-muted` does not exist.** `style="background:var(--surface-muted, #f8fafc);"` — theme.css defines `--page-bg/--card-bg/--border/--text/…` but no `--surface-muted` (grep over `static/` and `templates/`: only this one use), so the near-white fallback applies in BOTH modes; under `html.dark` (`--text: #e2e8f0`, `theme.css:46`) the week label renders light-on-light. The inline background also permanently outranks `.table tbody tr:hover` (`theme.css:298`), whose adjacent comment at `:299-301` explicitly warns against inline row backgrounds. Fix: `background:var(--page-bg);` (light #f6f7f9 / dark #0f172a — adapts to both scopes), or add a `.row-group` class to theme.css next to the existing `.row-warn` precedent.
- **`templates/projects/resource/resourceprofile/detail.html:11` — the "Book" deep link passes `?resource=<pk>`, which `ral_create` never reads.** `ral_create` builds `initial={"project": request.GET.get("project", "")}` only (`ResourceAllocations.py:79-80`), so clicking Book on a resource page opens the allocation form with an **empty Resource select** — the button's entire purpose defeated. Fix: read `?resource=` into `initial` (mirror `rte_create`'s `?resource=`, `ResourceTimeEntries.py:141-142`) or drop the param from the link. (Lane 1's I3 flags the same defect from the view side — one fix covers both lanes.)

### Minor

- **Edit/Delete offered on locked rows.** `resourcetimeentry/detail.html:28` (Edit link) and `:62-67` (Delete) render unconditionally, and the register's row pencil/trash (`resourcetimeentry/list.html:94-97`) render for `approved`/`rejected` entries — the views refuse them all (`ResourceTimeEntries.py:159-161, 172-174`), so every click is a bounce-with-message. The 7.2 frozen-row idiom hides the verbs exactly when the row is locked; wrap in `{% if obj.status == 'draft' or obj.status == 'submitted' %}`. (View-level guards are correct — offer-what-the-view-refuses only.)
- **RSP delete-confirm copy overstates the blast radius.** `resourceprofile/list.html:74` and `detail.html:75` say "Its bookings and time entries are deleted with it", but `ResourceAllocation.resource` is `SET_NULL` — bookings survive as placeholders; only time entries (`CASCADE`) die. Fix the sentence ("Its time entries are deleted with it; its bookings stay on as placeholders."). (Cross-ref lane 1 Minor.)
- **The capacity horizon select is JS-only.** `capacity_demand.html:11-25` relies on `onchange="this.form.submit()"` with no submit button and no Reset, so a no-JS user cannot change the horizon at all. A tiny `<noscript><button class="btn btn-outline">Apply</button></noscript>` (or an always-visible Apply button) restores parity.
- **Approve Week renders on every person-week subheader, including groups with zero submitted entries** (`resourcetimeentry/list.html:66-70`) — the POST then bounces with the pinned info refusal (`ResourceTimeEntries.py:261-263`). The contract pins the button unconditionally (a Django template cannot express "any entry in group.list is submitted" without a custom filter), so this is contract-conformant; noted for the fixer pass if a `submitted_count` annotation is ever threaded through `extra_context`.

### Verified correct

- **Every `{% url %}` resolves.** All rsp_*/ral_*/rte_* names used exist in `apps/projects/urls/ResourceManagement/*.py` (25/25 routes incl. `rte_approve_week`, whose `week/` literal precedes the `<int:pk>/` routes); the cross-links `projects:overview`, `projects:prj_detail`, `projects:prq_detail`, `projects:tsk_detail` exist in the 7.1/7.2 modules, and the lens links `hrm:employee_detail` (`apps/hrm/urls/EmployeeManagement/Detail.py:8`) and `hrm:employeeskill_list` (`apps/hrm/urls/WorkforcePlanning/Employeeskill.py:8`) resolve. The Approve-Week URL args (`group.list.0.resource_id group.list.0.iso_year group.list.0.iso_week`, `list.html:67`) match route 18's `(resource, year, week)` order exactly.
- **Context keys ↔ views, both directions.** Every variable the templates consume is passed: `q`/`object_list`/`page_obj` from `crud_list` (`apps/core/crud.py:166`); `resource_type_choices`/`status_choices`/`org_units` (`ResourceProfiles.py:27-31`); `booking_status_choices`/`projects`/`project_requests`/`resources` (`ResourceAllocations.py:54-59`); `successor`/`resources` on ral_detail (`:91-95`); `status_choices`/`resources`/`projects`/`actuals_rows`/`actuals_year`/`actuals_week` (`ResourceTimeEntries.py:115-122`); `form`/`obj`/`is_edit` from `crud_edit` (`apps/core/crud.py:212`); `weeks`/`week_windows`/`capacity_rows`/`over_count`/`demand_rows`/`gap_count` (`CapacityDemand.py:100-107`). No template var with no source; no dead context key.
- **Design-system conformance.** All ten pages extend `base.html` with page-header/breadcrumb (rooted at `projects:overview`, `.sep` separators)/page-actions; `card/card-header/card-body`; `filter-bar` GET forms; `table-wrap`+`.table` with trailing `th.table-actions`; `detail-grid` on all four detail pages; shared `form.html` with non_field_errors loop + `form-grid` + Save/Cancel; `.empty-state` rows; the pinned `{% include "partials/pagination.html" %}` on all three lists (the partial is L9-guarded and preserves `q`+filters across page links). Every modifier class exists in theme.css (grep-verified): `.fw-600`, `.text-muted`, `.req`, `.form-help/-error/-label/-input/-select/-textarea`, `.btn-outline`, `.btn-icon.danger`.
- **Badge discipline (L33).** Only colour-named variants are used — grep over the sub-module counts exactly `badge-amber/green/info/muted/red/slate` (0 × `-success/-danger/-warning`); every status map has the `{% else %}`+`get_*_display` fallback branch; the pinned maps match (`internal→info, contractor→amber, freelancer→muted, consultant→slate`; the six booking statuses incl. `completed→badge-info` per divergence 7; the four RTE statuses; active→green/inactive→muted) plus the contract's `{% if obj.is_live %}` extra `badge-info` "Live".
- **Traps avoided.** No nullable FK inside a `|default:` argument (the `|date:"…"|default:"—"` pairs sit on plain date/datetime fields, which render `''` then trigger the default; every FK deref is guarded by its `*_id` test, `successor` by `{% if successor %}`); `{% csrf_token %}` on all POST forms (3 register deletes + 3 detail-page deletes, the 5 RAL verb forms, submit/approve/reject, and Approve Week); the `{% regroup object_list by week_key %}` is safe (`Meta.ordering` keeps person-weeks contiguous under all filters since none reorder; `group.list.0` accessors only reached for non-empty groups; `{% empty %}` inside the group loop covers an empty page with colspan 7).
- **Admin-gating idiom.** Assign/Substitute (`resourceallocation/detail.html:17-42`), Approve/Reject (`resourcetimeentry/detail.html:16-27`) and every Approve-Week button (`resourcetimeentry/list.html:66-70`) are wrapped in `{% if request.user.is_superuser or request.user.is_tenant_admin %}` (the mst_achieve precedent; `is_tenant_admin` is a real `User` field, `apps/accounts/models.py:68`), while commit/complete/cancel/submit stay member-level — exactly the contract's split.
- **Filters reflect request.GET.** All FK selects compare with `|stringformat:"d"` (`org_unit`, `project`, `project_request`, `resource` on both lists) and re-select after submit; enum compares are `== val`; the `placeholder`/`is_live` selects use the `"True"`/`"False"` string literals the view parses (`ResourceAllocations.py:37-46`); the year/week number inputs echo `request.GET`; every select and both number inputs carry an `aria-label`; the `q` placeholder copy matches the contract ("Search name, role or skills…" verbatim on rsp).
- **Column/colspan math.** theads are 7/7/7 columns with `{% empty %}` colspans matching (7, 7, 7); regroup subheader rows colspan 7; actuals empty colspan 5; demand empty colspan 6; the capacity board's empty colspan is computed (`week_windows|length|add:"1"`, else 2) to survive an empty horizon.
- **Inline verb selects are well-formed.** Both `<select name="resource">` elements sit INSIDE their `<form>`s with `{% csrf_token %}`, are aria-labelled, iterate the `resources` helper queryset, and Substitute excludes the current resource (`{% if res.pk != obj.resource_id %}`, `detail.html:36`); the reject form carries the contract's `<textarea name="reason">` (`resourcetimeentry/detail.html:21-25`).
- **Board page (`capacity_demand.html`).** The caveat sentence is verbatim (`:32`); the demand intro is verbatim (`:70`); over/gap alert lines use `badge-red` summaries with `|pluralize` and green all-clear states; `demand_url_name`/`demand_url_pk` are always paired and the template guards on the name (`CapacityDemand.py:83-95`, template `:83`); the demand status map covers requested/soft with an else fallback; `id="actuals"` and `id="demand"` anchors exist for the LIVE_LINKS fragment; variance badges are `badge-red` when over plan / `badge-green` on-or-under (`list.html:126-127`); titles are exact ("Resource Pool", "Allocations", "Time Entries", "Capacity &amp; Demand"); empty-state icons/copy match the contract (`users`, `calendar-range`, `clock` + the pinned sentences).
- **Dark/RTL.** No raw Tailwind color utilities anywhere in the sub-module (the single inline color is the flagged subheader); layout uses flex/grid with no left/right hard-coding; tables all sit in `.table-wrap`.

**Praise:** the weekly lens in `resourcetimeentry/list.html` is the best template work in the changeset — a regroup subheader carrying the admin-gated Approve-Week verb with correctly-ordered 3-arg URL, plus an actuals-vs-plan card driven by the same `?year`/`?week` pair as the register filter and anchored `id="actuals"` for deep-linking.
- **`apps/projects/views/_helpers.py:3-6` module docstring is stale after the 7.3 additions (Low).** It still says "The first three here are the filter-dropdown builders … the fourth, `critical_path_ids`, is 7.2's critical-chain pass" — but the module now carries six helpers (`org_units`, `clients`, `projects`, `resource_profiles` at :48, `project_requests` at :58, `critical_path_ids`). This is exactly the staleness class 7.2's fixer pass fixed for the fourth helper (commit `b2c0e06d`); the 7.3 commit (`e29df2c0a`) added two helpers without bumping the enumeration.
- **`project_requests(tenant)` has exactly one consumer, against the file's own placement rule (Low — defensible).** `_helpers.py:1-2` states "A helper used by a single entity stays in that entity's module", yet `project_requests` (`_helpers.py:58-67`) is consumed only by `ral_list` (`views/ResourceManagement/ResourceAllocations.py:19,57`). `resource_profiles` legitimately qualifies (three consumers: `ral_list`, `ral_detail`, `rte_list`). Keeping the request dropdown next to `resource_profiles` is coherent (the demand lens pair), but by the documented rule it belongs in the entity module — one line to note for the fixer pass.
- **No `test_resource_*` suite exists yet (Note — routing, not a defect).** The contract pins test subslug `resource` (fixtures `resource_*`, tests `test_resource_*`, contract line 21), but `apps/projects/tests/` contains only the 7.1 files plus `test_planning_models.py`, and `conftest.py` has no `resource_*` fixtures. The only 7.3 verification artifact is `temp/smoke_73.py` (+ `smoke_73_reset.py`). The test-writer lane should add the suite.
- **Known Phase-7 item, noted per charter (not counted):** `.claude/skills/projects/SKILL.md:18` still says "As-built: 7.1 only. 7.2–7.19 are roadmap" — it was already stale for 7.2 before this changeset; 7.3 documentation belongs to the deferred skill/docs pass.

### Verified aligned

- **Bullet coverage — all five NavERP.md 7.3 bullets map to live pages that realize them (`NavERP.md:1148-1152` → `apps/core/navigation.py:1731-1745`):** (1) Resource Pool & Skills Inventory → `projects:rsp_list` — the pool register delivers people/roles/teams/capacity (`templates/projects/resource/resourceprofile/list.html:51-69`) and the competency-matrix/certifications promise is the HRM lens: `rsp_detail` deep-links `{% url 'hrm:employeeskill_list' %}?employee={{ obj.employee_id }}` inside `{% if obj.employee_id %}` (`resourceprofile/detail.html:52-64`), and the lens is real — `hrm:employeeskill_list` exists (`apps/hrm/urls/WorkforcePlanning/Employeeskill.py:8`) and its view appends the `("employee","employee_id",True)` filter for tenant admins (`apps/hrm/views/WorkforcePlanning/Employeeskill.py:28-29`), exactly the admin-only narrowing contract divergence #10 documents. Availability calendars appear only as the contractor engagement window (`available_from/to`, `ResourceProfiles.py:58-61`) — the contract's documented stand-in ("no availability engine beyond the contractor engagement window", contract line 10). (2) Resource Allocation & Leveling → `projects:capacity_demand` — capacity planning + over-allocation alerts are real (`CapacityDemand.py:63-73`, alert line `capacity_demand.html:33-37`); "automatic smoothing algorithms" is deliberately not built (contract scope guard line 27), and the page says the manual rebalance path verbatim (`capacity_demand.html:8`). (3) Team Assembly & Role Assignment → `projects:ral_list` — named bookings, generic placeholders (NULL resource), and the substitution workflow all exist (`ral_assign`/`ral_substitute`, `ResourceAllocations.py:116-190`). (4) Resource Forecasting & Demand Planning → `projects:capacity_demand#demand` — pipeline-vs-capacity demand section (`CapacityDemand.py:75-98`), hiring trigger = `is_gap`/`gap_count` (`capacity_demand.html:71-75,91`), contractor engagement = `resource_type="contractor"` + window fields. (5) Time Tracking & Timesheets → `projects:rte_list` — daily entry, the weekly lens via `{% regroup object_list by week_key %}` (`resourcetimeentry/list.html:60-73`), approval routing (submit/approve/reject/approve_week), and the actuals-to-plan section `id="actuals"` (`ResourceTimeEntries.py:27-73`, `list.html:111-137`).
- **Sidebar reality — all six `LIVE_LINKS["7.3"]` targets resolve.** Ran `reverse()` under `config.settings_test` with `_route_name`'s suffix semantics (`navigation.py:1888-1895`): `projects:rsp_list` → `/projects/resource-profiles/`, `projects:capacity_demand` → `/projects/capacity-demand/`, `projects:ral_list` → `/projects/allocations/`, `projects:capacity_demand#demand` → `/projects/capacity-demand/#demand`, `projects:rte_list` → `/projects/time-entries/`, `projects:rte_list?status=submitted` → `/projects/time-entries/?status=submitted` — no NoReverseMatch. The five bullet names are byte-verbatim `NavERP.md:1148-1152`, and `parse_catalog` appends non-bullet leaves like "Time Approvals" (`navigation.py:1845-1848`). The block sits immediately after 7.2's, at `navigation.py:1731`, matching the contract.
- **Doc agreement on what 7.3 is:** `NavERP.md:1147-1152` (five bullets) ↔ contract (`contract-projects-7.3.md` mirrors them verbatim in the LIVE_LINKS block and pins every namespace) ↔ as-built (3 models / 25 routes / 9 verbs all byte-match the contract tables — spot-verified independently: models, forms, all four url modules, views, re-export blocks, admin, migration 0004's 12 index names, seeder block, Overview counts).
- **Cross-module duplication check — the documented exclusions hold in the actual code:** no second person master (`ResourceProfile` FKs `hrm.EmployeeProfile` + `core.Party`, `ResourceProfiles.py:35-42`; no new person table); no duplicate skill table (`hrm.EmployeeSkill` at `apps/hrm/models/WorkforcePlanning/Employeeskill.py:5` is the only skill table; `skill_summary` is a CharField quick-filter, `ResourceProfiles.py:49-51`); no duplicate timesheet header (`crm.Timesheet` `apps/crm/models/ProjectDelivery/Timesheets.py:5` and `hrm.Timesheet` `apps/hrm/models/TimeTracking/Timesheet.py:8` are the only header models; 7.3 adds flat entries only); no money columns anywhere in the three models (no rate/billable/cost field — full field lists read); `crm.ResourceAllocation` [RA-] (`ResourceAllocation.py:13`, prefix RA) and `crm.Timesheet` [TS-] are untouched since BASE (empty `git diff` on `apps/crm/models/ProjectDelivery/`, `apps/hrm/models/WorkforcePlanning/Employeeskill.py`, `apps/hrm/models/TimeTracking/`), with the two-models-one-name ruling documented in the model docstring (`ResourceAllocations.py:10-15`); a `NUMBER_PREFIX` grep confirms `RSP`/`RAL`/`RTE` are unique repo-wide (the other `R*` prefixes — RINV/RPT/RCPT/RA/REG/RSV/… — are all distinct strings).

---

## 4. performance-reviewer

Verdict: Query shape is disciplined — filters land before pagination, every queryset is evaluated once, both computed pages are linear and week-bounded, the seeder is one transaction with numbering-justified per-row saves — but `ResourceProfile.name`'s two-hop walk (`employee → party`) is select_related-ed at only 3 of its ~7 render sites, so the allocation register and every resource dropdown/form label pay a per-row / pool-size N+1 that one token each fixes.

### Critical

None found. No super-linear or unbounded path: `_actuals_rows` and `capacity_demand` are cold-ish but member-triggerable, and both are bounded (see Verified correct); the worst hot-path render is the ral_list N+1 below, which is linear in page rows + pool size, not multiplicative.

### Important

- **`apps/projects/views/ResourceManagement/ResourceAllocations.py:34-35` + `templates/projects/resource/resourceallocation/list.html:85` — the allocation register renders `{{ obj.resource.name }}` through an unselected two-hop walk: up to +30 queries per 15-row page.** `ral_list` selects `("project", "project_request", "project_task", "resource")` — the `ResourceProfile` row itself is joined, but `name` is a property (`apps/projects/models/ResourceManagement/ResourceProfiles.py:78-85`) that walks `self.employee` → `self.employee.party.name` (2 more queries) for internal resources, `self.party.name` (1) for externals. Every named row on the page pays it (only the Placeholder rows are free), and the exact 7.1-precedent shape exists two modules over: `rte_list` already selects the full walk (`ResourceTimeEntries.py:79` `resource__employee__party, resource__party`). Fix: `select_related("resource__employee__party", "resource__party")` at `ResourceAllocations.py:35`.
- **`apps/projects/views/_helpers.py:48-55` — `resource_profiles()` (and, through `ResourceProfile.__str__`, every Resource `<select>` label) walks the same two hops per pool row on four pages and four form renders: pool-size × (1–2) queries per render.** The helper is bare `ResourceProfile.objects.filter(tenant=tenant)`; its consumers render `res.name` in the resource filter dropdown of `ral_list` (`ResourceAllocations.py:58` → `resourceallocation/list.html:42`), `rte_list` (`ResourceTimeEntries.py:117` → `resourcetimeentry/list.html:34`) and twice on `ral_detail` (`ResourceAllocations.py:93` → `resourceallocation/detail.html:24` and `:36`). Separately, the Resource ModelChoiceField on the allocation and time-entry forms has no `label_from_instance` override (grep: none repo-wide), so each `<option>` label calls `str(ResourceProfile)` = `f"{number} · {name}"` (`ResourceProfiles.py:75-76`) — and `apps/core/forms/_common.py:52-55` scopes the queryset with `.filter(tenant=…)` but no `select_related` — so every New/Edit Allocation and every Log Time render (the module's most-clicked form) walks `employee__party` per pool row too. For a 100-person pool that is ~150–200 extra queries on a dropdown page or form render. Fix: one line in the helper — `return ResourceProfile.objects.filter(tenant=tenant).select_related("employee__party", "party")` — and in the two 7.3 forms re-set the field queryset (or teach `TenantModelForm` to select_related its scoped choices; that is an app-wide change, so the per-form line is the safe move).

### Minor

- **Same one-token walk, single-row/bounded sites (each +1/+2 queries per render):** `ral_detail` selects `"resource"` but renders `obj.resource.name` (`ResourceAllocations.py:88-89` vs `resourceallocation/detail.html:88`, +2) and `obj.requested_by.username` (`detail.html:106`) with `requested_by` not in the select_related (+1); `rte_detail` selects `"resource"` and renders `obj.resource.name` (`ResourceTimeEntries.py:149-152` vs `resourcetimeentry/detail.html:37`, +2); `_actuals_rows`' approved queryset selects `"resource", "project"` (`ResourceTimeEntries.py:36-39`) but the actuals card renders `row.resource.name` (`resourcetimeentry/list.html:121`) — +2 per distinct resource in one ISO week's table. All bounded, all fixed by extending the existing select_related tuples.
- **`% of Capacity` bookings pay +1 query each in both computed pages:** `planned_hours` derefs `self.resource.weekly_capacity_hours` for pct units (`apps/projects/models/ResourceManagement/ResourceAllocations.py:126-130`), but `capacity_allocs` (`CapacityDemand.py:54-58`) and `_actuals_rows`' `live_allocs` (`ResourceTimeEntries.py:49-53`) don't `select_related("resource")` — the FK object caches on the instance after the first week's call, so the cost is 1 query per pct-unit soft/firm booking per render (cached across the 13-week loop; `_actuals_rows` only calls it for pairs that logged time, `ResourceTimeEntries.py:56`). The demand section already does it right (`CapacityDemand.py:80`). This is lane 1's routing note, verified and quantified. Fix: add `"resource"` to both querysets.
- **`?year=`/`?week=` on the register compile to function-wrapped SQL no b-tree can serve, and the actuals window has no dedicated date index (bounded, note-only).** Verified against the installed backend: `entry_date__week` → `WEEK(entry_date, 3)` and `entry_date__iso_year` → `TRUNCATE(YEARWEEK(entry_date, 3), -2) / 100` (django/db/backends/mysql/operations.py:52-60) — the crud_list filters (`ResourceTimeEntries.py:113-114`) therefore scan the tenant's rows however many indexes exist. The actuals window's plain gte/lte (`ResourceTimeEntries.py:38`) can use only the `tenant` prefix of `rte_tnt_res_date_idx`/`rte_tnt_prj_date_idx` (entry_date is their 3rd column). Tenant-bounded and paginated, so acceptable; a `("tenant", "entry_date")` index would serve the actuals window and any future date-range lens if the register grows.
- **Meta.ordering direction mismatches (note-only, both tenant-bounded and paginated):** RTE's `["resource_id", "-entry_date", "-id"]` (`models/ResourceManagement/ResourceTimeEntries.py:59`) is mixed-direction — the all-ASC `rte_tnt_res_date_idx` cannot serve ASC-resource/DESC-date, so unfiltered register renders filesort the tenant's matching rows (the `{% regroup %}` correctness does still hold). RAL's `["-start_date", "-id"]` is served for filter + dominant sort by `ral_tnt_start_idx` (backward scan) with the `-id` tiebreak forcing a sort of per-date ties only. RSP's joined-name ordering LEFT-JOINs on every rowset but serves a people-sized pool and is contract-pinned.
- **`rte_approve_week` writes N UPDATEs where one `bulk_update` fits** (`ResourceTimeEntries.py:265-270`: per-entry `.save(update_fields=…)` inside the atomic). Bounded by one person-week (≤ ~40 rows), already one audit row, so this is symmetry polish, not a defect: `bulk_update(entries, ["status", "approved_by", "approved_at", "updated_at"])` after stamping `entry.updated_at = now` (bulk_update skips `auto_now`) collapses it to 1 query.

### Verified correct

- **Filters before pagination; count() not len(); no aggregate-in-a-loop; no list()-to-count.** `crud_list` applies search + all filters (`apps/core/crud.py:117-164`) before `Paginator` (:165, SQL COUNT via `paginate` :22-34); the hand-parsed `?placeholder=`/`?is_live=` lenses run before `crud_list` too (`ResourceAllocations.py:37-46`). The only two `len()` uses in 7.3 run over already-fetched lists (`len(entries)` after `list(...)`, `ResourceTimeEntries.py:257-273`; `gap_count`, `CapacityDemand.py:106`) — correct per the reuse rule. No `.aggregate()` per-row anywhere; the only aggregates are the Overview flat COUNTs.
- **`_actuals_rows` (`ResourceTimeEntries.py:27-73`) — 2 queries for the section, each evaluated exactly once.** `approved` (:36-39) iterated once at :40; `live_allocs` (:49-53) iterated once at :54; the Python loop calls `planned_hours` only for (resource, project) pairs that logged approved time (guard at :56), so the planned-side cost is bounded by distinct pairs in one ISO week, not by live bookings; the per-pair sum is Decimal arithmetic, no DB; sort in Python (:72). The section renders on every `rte_list` render (bottom card) so nothing is computed and discarded.
- **`capacity_demand` (`CapacityDemand.py:31-107`) — 4 queries total and a clamped horizon.** profiles (:52-53, name walk selected) + capacity_allocs (:54-58) + demand ×2 (:81-82, disjoint project-null halves, project-linked first). The per-resource-per-week loop (:64-73) is profiles × weeks(≤13) × their allocations of pure Decimal math after fetch; `allocs_by_resource` grouping (:59-61) keeps it linear; `?weeks=` junk falls to the default (`as_db_int(...) or DEFAULT_WEEKS`, :36) and clamps 4..13 (:37); the demand_hours loop (:96-97) is demand_rows × weeks; `over` compares against the local `weekly_capacity_hours` (:69). Nothing super-linear, nothing unbounded.
- **Column-by-column select_related coverage on the pages not flagged above:** `rsp_list`/`rsp_detail` select `employee__party, party, org_unit` — every column of `resourceprofile/list.html:53-79` and every `dd` of `detail.html:19-47` renders from those three joins or local fields (the `name` walk included); `rte_list` selects `resource__employee__party, resource__party, project, project_task` (`ResourceTimeEntries.py:78-80`) — covers the row Resource cell (:77), the regroup subheader (:65) and the Approve-Week confirm string (:67) in full; `capacity_demand`'s profiles cover the board's name/number/capacity row (`capacity_demand.html:49`) and the demand rows render only local fields + `demand_label` (project/project_request selected, `CapacityDemand.py:80`).
- **Filter→index map, migration 0004 (12 named indexes byte-match the Meta declarations; no hot filter left uncovered beyond the Minor 3 shape):** rsp `?resource_type` → `rsp_tnt_rtype_idx`, `?status` → `rsp_tnt_status_idx`, `?org_unit` → no composite, but both columns carry single-column b-trees (Django's default FK index + `tenant`'s explicit `db_index=True`, `models/_base.py`) and the pool is people-sized; ral `?project` → `ral_tnt_project_idx`, `?project_request` → `ral_tnt_request_idx`, `?resource` and the `resource__isnull` placeholder lens → `ral_tnt_resource_idx` (NULL keys indexed), `?booking_status` and the is_live lens's status arm → `ral_tnt_status_idx`, ordering `-start_date` → `ral_tnt_start_idx`; rte `?status` → `rte_tnt_status_idx` (also serves `_actuals_rows`' status filter), `?resource` → `rte_tnt_res_date_idx` prefix, `?project` → `rte_tnt_prj_date_idx` prefix; `ral_substitute`'s successor lookup and the search icontains are house-wide patterns, unchanged.
- **Seeder `_resourcing` family (`apps/projects/management/commands/seed_projects.py:326-527`):** guard `.exists()` at :348; ONE `transaction.atomic()` (:362) wraps pool + allocations + request demand + time entries; 31 per-row `.save()` calls (5 profiles + 9 allocations + 1 demand + 16 entries), each costing 1 `next_number` SELECT + 1 INSERT — the SELECT is index-satisfiable by the unique `(tenant, number)` (apps/core/utils.py:57-61, the documented max+1 pattern) and numbering genuinely needs the save, accepted per charter; ZERO `write_audit_log` calls in the family (the seeder's only audit rows are 7.1's at :840/:852/:904); 3 COUNT queries for the success line and one guard query — all once per run, not per row.
- **Overview 7.3 hunks (`apps/projects/views/ProjectInitiation/Overview.py:65-67`):** three flat `.count()`s, one per table, no join they could share, matching the file's own one-COUNT-per-table rule and its comment (:64).
- **No prefetch_related opportunities missed:** no 7.3 template iterates a reverse or M2M manager (the `{% regroup %}` iterates the already-fetched page; `substituted_by` is resolved once via `.order_by("pk").first()`, `ResourceAllocations.py:94` — the released row can gain at most one successor by the verb guards).
- **Field loading:** no `.only()/.defer()` anywhere in 7.3 — correct to omit: the rowsets are narrow, and the one TextField on each model (`notes`) never renders in any list template, so there is nothing worth deferring.

### Routing

- test-writer: a `django_assert_max_num_queries(10)` on `ral_list` (5-resource pool, mixed named/placeholder bookings) pins Important 1 + 2 — today the page renders ≈7 base queries (count, page, 3 dropdowns, request helpers) plus up to 30 row-walks plus 2×pool dropdown-walks; the same assertion on `rte_list` would guard the actuals-section walk (Minor 1) from regressing.
- fixer pass: Important 1 and Important 2 are each a one-line select_related change (register tuple at `ResourceAllocations.py:35`; helper return at `_helpers.py:55`) plus the per-form queryset line in the two 7.3 forms — the Minor 1 sites are the same token added to three existing tuples.

---

## 5. qa-smoke-tester

Verdict: Runtime pass — 108 contract-diff checks green against the live app as `admin_acme` (every refusal/info text byte-matches the contract table with the right toast severity, both computed pages match an independent re-implementation of the contract math, all filter lenses return exactly the recomputed sets, sidebar/overview truthful, no 500 anywhere), DB left in its pristine seeded state; no Critical or Important of my own — three Minors.

Method: `temp/qa73_review.py` (gitignored, kept for the fixer to re-run: `venv\Scripts\python.exe temp/qa73_review.py`). READ-ONLY on seeded rows — every POST targets a refusal path (no state change) except one throwaway RAL created, refused in its cancelled state, and deleted again (register verified back at the seeded 10 rows afterwards). Expectations are recomputed independently from the DB rows (own `planned_hours`/actuals replicas written from the contract text), never from the code under test; lens/page values are read from `response.context` + rendered HTML, not assumed. No 7.4 code touched. `manage.py check` → "no issues (0 silenced)"; `makemigrations --check --dry-run` → "No changes detected" on the shared tree at HEAD.

### Critical

None found. No 500s on any edge: week=0 / week=99 / week=2026 / fake ISO week 2025-W53 on `rte_approve_week`, `?week=99` on the register, every refusal POST — all returned a polite `messages` redirect. No mutation escaped the script: a post-run re-probe shows all 5 RSP / 10 RAL / 16 RTE rows byte-identical to the seeder's states (statuses, resources, dates).

### Important

None found.

### Minor

- **The actuals card's Planned column renders the unquantized zero sentinel — `0h` next to `2.00h` in the same table.** `apps/projects/views/ResourceManagement/ResourceTimeEntries.py:24` (`_ZERO = Decimal("0")`) and `:64` (`planned_by_pair.get((resource_id, project_id), _ZERO)`) leave the no-live-booking rows' `planned_hours` at integer-shaped `Decimal("0")` while `actual_hours`/`variance` are quantized to 0.01 at `:68`/`:70`; `templates/projects/resource/resourcetimeentry/list.html:123` renders `{{ row.planned_hours }}h` verbatim. Observed HTML for the default week's only row: `<td>0h</td><td>2.00h</td><td>+2.00h over plan</td>`. Cosmetic only (the arithmetic and the badge are correct); fix is one `.quantize(Decimal("0.01"))` (or the house `q2`) on the row-build at `:69`.
- **The over-allocation alert — the capacity board's headline feature — never fires under seeded data, so its red branch is seed-dead.** Hand-computed from the seeded rows: the busiest cell in any horizon is RSP-00005's 18.67h (the 40h total-hours booking spread over its own 15-day window, W39) against 40h capacity, and the 24h part-timer peaks at 12h (50% of capacity) — so `over_count = 0` for every horizon length and only the green all-clear renders (`capacity_demand.html:36`); the red "N over-booked resource-weeks" branch (`:33-34`) is unreachable from seed data. The branch logic itself is verified correct (all 5×8 `over` flags match my independent recompute), and the contract's "capacities varied so over-allocation is honest" reads as computation-honesty — but one seed number (e.g. RAL-00001 16h/wk → 48h/wk) would exercise the alert state no reviewer or user ever sees today.
- **Runtime-confirmed lane 3's I2 (Assign/Substitute `{% if %}` precedence) — offered-but-always-refused verb buttons.** GET `ral_detail` for RAL-00003 (named, soft) renders the Assign verb form (`/projects/allocations/<pk>/assign/`), whose POST always refuses on a named row — the refusal text itself verified verbatim on RAL-00001 ("That allocation already names a resource — use Substitute to replace it."). Symmetric Substitute-on-firm-placeholder not directly POSTed (no firm placeholder exists in seed); one template fix covers both lanes — no separate action needed here beyond the confirmation.

### Verified correct

- **All 21 refusal/info texts byte-match the contract's verb table, with the correct toast severity class each time** (error vs info checked via the rendered `<div class="toast …">` markup, not just substring): `ral_assign` named ("That allocation already names a resource — use Substitute to replace it."), bad resource ("Choose a resource to assign."), cancelled placeholder ("A cancelled allocation cannot be staffed."); `ral_substitute` placeholder ("That allocation is a placeholder — assign a resource to it instead."), completed ("Only a soft or firm booking can be substituted — this one is completed."), current resource (info, "That is already the assigned resource."), junk resource ("Choose the replacement resource."); `ral_commit` firm ("…this one is firm booked." — the two-word `display.lower()` form is the contract's literal `{display.lower()}`, not a drift) and cancelled; `ral_complete` released/cancelled; `ral_cancel` completed/released and the already-cancelled info ("That allocation is already cancelled."); `rte_submit` already-submitted (info)/approved/rejected; `rte_approve`/`rte_reject` on a draft; `rte_edit` GET and `rte_delete` POST locks ("An approved or rejected entry is locked — it cannot be edited./deleted." — row persists); the create label "Allocation RAL-00011 created." carries the contract's "Allocation" label.
- **`rte_approve_week` edge ladder, all polite redirects, never 500:** week=0 and week=99 → "Week must be between 1 and 53."; a bogus middle arg (week=2026) hits the same clamp; 2025-W53 → "2025 week 53 is not a real ISO week." (2025 has 52 ISO weeks); 2026-W53 — a REAL 53-week year — passes the guards and gives the contract's zero-rows info "No submitted entries for Olivia Martin in week 53."; last week (queue already approved) gives the same info for week 36. No entry was ever mutated by any of these.
- **`capacity_demand` matches a hand/independent recompute exactly:** `over_count = 0` and all 5×8 `cells` (planned Decimal + over flag) equal my own re-implementation of the contract's `planned_hours` — spot checks RSP-00001 W37 = 16.00h, RSP-00004 W37 = 12.00h (50% of the 24h part-timer), RSP-00005 W39 = 18.67h (total-hours own-window spread). Demand section: 5 rows in the contract's order (project-linked by start_date then −id, then request-linked) = RAL-00005, RAL-00003, RAL-00009, RAL-00004, RAL-00010; `gap_count = 3` (RAL-00004/05/10) with the coverage line "3 unassigned bookings"; `demand_hours` RAL-00003 = 66.86, RAL-00009 = 40.01 (the honest sum of per-week q2 rounding of a 40h total — arithmetic verified, not a defect), RAL-00010 = 111.43, horizon-dependent at `?weeks=4` (31.43); `is_gap == (resource_id is None)` on every row; caveat and demand-intro sentences verbatim; the `id="demand"` anchor present.
- **`rte_list` actuals-to-plan recomputed by hand, two windows:** default = current ISO week (header "Actuals vs plan — ISO week 37, 2026") with exactly one row — RSP-00001 / Non-project time / 2.00 actual / 0 planned / +2.00 red over-plan badge (the seeded approved non-project 2h entry); `?year=2026&week=36` moves the window and yields exactly three pairs in variance-desc order — RSP-00002 12.00/6.86/**+5.14** (its 12h/wk booking started Thursday → 4/7 prorated, red badge), RSP-00003 12.00/12.00/0.00, RSP-00001 6.00/16.00/−10.00 (green) — all values equal to my independent recompute; the section ignores `?status=` (identical rows with `?status=submitted`); `?week=99` falls back to the current week without 500.
- **Filter lenses return exactly the recomputed sets:** `?placeholder=True` = {RAL-00004, RAL-00005, RAL-00010} (exactly 3), `?placeholder=False` = the 7 named rows; `?is_live=True` = the recomputed live set {RAL-00001/02/03/05/08} (RAL-00009 correctly excluded — starts +7 days), `?is_live=False` = its exact complement, and the compound `?placeholder=True&is_live=True` = the intersection {RAL-00005}; `?status=submitted` = exactly {RTE-00010, RTE-00011, RTE-00012} (3), `?status=rejected` = exactly {RTE-00015}; `rsp_list?resource_type=contractor` = exactly {RSP-00005}. All sets compared as pk sets from `response.context["object_list"]` against DB recomputation, not HTML greps.
- **Sidebar + overview:** the 7.3 subgroup renders with all six links (five bullets + Time Approvals leaf, incl. the `#demand` and `?status=submitted` fragments) and is pill-free/`nav-roadmap`-free — the same live treatment as the 7.2 subgroup (compared directly); overview stat cards render the exact DB counts 5 / 10 / 16 next to "Resources"/"Allocations"/"Time entries", and all four quick links resolve in the HTML.
- **Workspace hygiene:** `manage.py check` ("no issues (0 silenced)") and `makemigrations --check --dry-run` ("No changes detected") clean on the shared tree at HEAD; the throwaway row was deleted and a full re-probe confirmed all seeded rows unchanged (RAL statuses/resources/dates and RTE statuses byte-identical to the seeder); Globex untouched; nothing committed.

---

## 6. security-reviewer

Verdict: Tenant isolation holds end-to-end (every queryset, every fetch, both POSTed-resource resolutions, both `_helpers` functions), the nine verbs carry exactly the pinned gate chains with all-POST state changes, forms close the crafted-POST boundary on the right FK lists, and there is no `|safe`/`style=`-injection/CSRF/SQL/mass-assignment surface — zero Critical/Important of my own; one Minor concurrency finding, plus a security-angle confirmation of the cross-lane confirm() finding (not re-numbered).

### Critical

None found.

### Important

None found.

- **Cross-reference (verified from the security angle, owned as lane 1 I1 / lane 3 I1 — not re-counted):** `templates/projects/resource/resourcetimeentry/list.html:67` interpolating `{{ group.list.0.resource.name }}` into the Approve-Week `confirm()` string is the changeset's one stored-XSS-shaped surface, and the security reading makes it worse than cosmetic: `resource.name` resolves to a member-writable `core.Party.name` (`apps/projects/models/ResourceManagement/ResourceProfiles.py:79-85`), the HTML-attribute decode order (L42) restores a bare `'` before the JS engine compiles the handler, and the injected script then runs **in a tenant admin's authenticated session on the approval-queue page** — where it can read the DOM and POST the admin-gated verbs (`rte_approve`, `rte_reject`, `ral_substitute`) using the admin's own CSRF cookie, i.e. the CSRF token provides no defense once script executes. Every other `confirm()` in the sub-module interpolates only the system-generated `{{ obj.number }}` (rsp list/detail, ral detail, rte detail — safe). The lane 1/3 fix (interpolate `resource.number`) closes both the broken-guard and the XSS case.

### Minor

- **`apps/projects/views/ResourceManagement/ResourceAllocations.py:147-183` — `ral_substitute`'s status guard is read at fetch time, outside the `transaction.atomic()` that releases the row and mints the successor, so two concurrent admin POSTs can release one booking and create TWO successors.** Both requests fetch `obj` (`booking_status="soft"`), both pass the `not in ("soft", "firm")` check (:152), both then enter the atomic block and each creates a successor carrying the original window/magnitude — the capacity board then double-counts the demand (and `over_count`/`gap_count` misreport), with one `substitute` audit row and one un-audited twin. `substitute_of` has no unique constraint to catch it, and the "one successor per released row" assumption the performance lane relies on (`ResourceAllocations.py:94`) only holds while the guard is un-raced. The house's own precedent for this exact double-mint class is the second in-flow guard (`prq_convert`'s docstring: "a double-click here would otherwise mint two projects", `apps/projects/views/ProjectInitiation/ProjectRequests.py:259-261`); `select_for_update` is also established house practice (119 files, e.g. `apps/hrm/views/AssetManagement/Asset.py:76`). Fix: re-fetch the row inside the transaction — `ResourceAllocation.objects.select_for_update().get(pk=obj.pk, tenant=request.tenant)` — and re-run the status check there (and/or refuse when `obj.substituted_by.exists()`). Admin-gated and race-window-only, hence Minor.

### Verified correct

- **Tenant isolation, end-to-end.** Every list/computed queryset filters `tenant=request.tenant`: `rsp_list` (`ResourceProfiles.py:18`), `ral_list` (`ResourceAllocations.py:34`) including the hand-parsed `?placeholder=`/`?is_live=` lenses (:37-46), `rte_list` (`ResourceTimeEntries.py:78`), `_actuals_rows`' two queries (`ResourceTimeEntries.py:36-39, 49-53`), `capacity_demand`'s three (`CapacityDemand.py:52-58, 76-80`), and both 7.3 `_helpers` functions return `.none()` for a tenant-less user and `filter(tenant=…)` otherwise (`views/_helpers.py:48-55, 58-67`). Every `get_object_or_404` carries `tenant=request.tenant` — the three details (`ResourceProfiles.py:57-59`, `ResourceAllocations.py:87-90`, `ResourceTimeEntries.py:149-152`), all nine verbs, `rte_edit`/`rte_delete`'s lock-guard fetches (:158, :171), and the shared `crud_edit`/`crud_delete`/`crud_detail` helpers (`apps/core/crud.py:198, 218, 226`). The POSTed resource pk in `ral_assign`/`ral_substitute` is resolved through `as_db_int` + `ResourceProfile.objects.filter(tenant=request.tenant, pk=…)` (`ResourceAllocations.py:126-128, 158-160`) — a foreign pk yields the "Choose a resource…" refusal, never the row. `rte_approve_week` resolves the person tenant-scoped (`ResourceTimeEntries.py:248`) and the bulk queryset is tenant+resource+status scoped (:257-260); the successor in `ral_substitute` is constructed with `tenant=obj.tenant` from the already-verified row (:172).
- **Forms — crafted-POST boundary.** `_reject_foreign` covers exactly the tenant-scoped FKs that render as fields: RSP `employee`/`party`/`org_unit` (verified `hrm.EmployeeProfile` is `TenantNumbered`; `core.Party`/`OrgUnit` carry `tenant`), RAL `project`/`project_request`/`project_task`/`resource`, RTE `resource`/`project`/`project_task` — and never the user FKs (`requested_by`/`approved_by` are `editable=False` and absent from all three `Meta.fields`). `TenantModelForm` auto-scopes every ModelChoiceField whose target has a tenant (`apps/core/forms/_common.py:52-55`), so the dropdowns and the crafted-POST re-check agree. No secret/credential/derived field is on any form; the verb-driven state (`booking_status`, `substitute_of`, `status`, all stamps, `decision_note`) is excluded, so the member-reachable `*_edit` path cannot bypass the state machine — and `admin.py` doubles down by freezing those same columns readonly (`apps/projects/admin.py:130-132, 141-143`).
- **AuthN/AuthZ gates.** All nine verbs match the pinned chains: `login_required` + `tenant_admin_required` + `require_POST` on assign/substitute (`ResourceAllocations.py:113-115, 143-145`) and approve/reject/approve_week (`ResourceTimeEntries.py:197-199, 217-219, 239-241`); `login_required` + `require_POST` on commit/complete/cancel/submit; all three deletes POST-only with `crud_delete`'s self-defending method check (`apps/core/crud.py:217-223`); list/detail/edit `@login_required`; all three creates guard `request.tenant is None` first → `dashboard:home` (`ResourceProfiles.py:37-39`, `ResourceAllocations.py:65-67`, `ResourceTimeEntries.py:128-130`). `tenant_admin_required` raises `PermissionDenied` → 403 for members (`apps/core/decorators.py:13-21`), and the templates hide the now-403 buttons behind the `is_superuser or is_tenant_admin` idiom (ral detail :17-42, rte detail :16-27, rte list :66-70).
- **Injection/XSS surfaces.** Grep-clean: zero `|safe`/`mark_safe`/`{% autoescape %}` across the 11 `templates/projects/resource/**` files; zero `csrf_exempt`/`.raw()`/`.extra()`/`cursor.execute` in the 7.3 code. `decision_note`, `notes` render through `|linebreaksbr` (escape-then-br, autoescaped — `resourcetimeentry/detail.html:56-57`, `resourceallocation/detail.html:107`, `resourceprofile/detail.html:47`); `{{ q }}`/`{{ request.GET.year }}`/`{{ request.GET.week }}` echoes are autoescaped; the only `style=` attributes are static literals (no user value reaches any `style=`, L26 clean); the demand table's `{% url r.demand_url_name r.demand_url_pk %}` renders only view-controlled literal route names (`CapacityDemand.py:83-95`).
- **Verb abuse / CSRF / numeric hardening.** Every state-changing endpoint is POST-only (GET → 405) and every POST form carries `{% csrf_token %}` (all 10 templates; the capacity horizon form is GET). `rte_approve_week` clamps the route ints: week `1..53` (`ResourceTimeEntries.py:249-251`) plus a `date.fromisocalendar` ValueError guard that politely refuses a bogus ISO year (:252-256) — `<int:…>` converters reject negatives/non-numerics at the URL layer. All hand-parsed ids go through `as_db_int` (resource pks :126/:158; `?year`/`?week` :85-86; `?weeks` :36) — no raw `int()` on any GET/POST value; `?year=0/9999` empties the register via `qs.none()` before the year-lookup bounds can raise (:92-94). The capacity horizon is clamped `4..13` (`CapacityDemand.py:36-37`). `crud_list`'s enum allow-listing + `as_db_int` guard every GET filter (`apps/core/crud.py:119-164`).
- **Mass assignment.** `request.POST` is read directly only into the pinned verb fields: the `resource` pk via `as_db_int` (assign/substitute) and `reason` → `decision_note` after `.strip()` (`ResourceTimeEntries.py:231`). Everything else flows through the three ModelForms, which exclude `tenant`, `number`, verb state, stamps and `requested_by`; `requested_by` is stamped server-side in `ral_create` (`ResourceAllocations.py:73`). No `initial=` value can inject a foreign row — `initial={"project": request.GET.get(…)}` only pre-selects among the tenant-scoped queryset's own options and is re-validated by `_reject_foreign` on POST.
- **Info leaks.** A cross-tenant `<int:pk>` 404s at `get_object_or_404(tenant=…)` before any verb logic, so refusal messages, success messages and audit rows can never name another tenant's row; message texts are static or own-tenant values; the three 7.3 ModelAdmins sit behind Django admin's own staff/superuser gate and expose `tenant` as a display column only; no `?next=`/open-redirect, no file upload, no secret on any page or in any message (L20/L25 clean).
- **Seeder.** `_resourcing` and its `_pool`/`_allocations`/`_time_entries` family write only `tenant=tenant` rows fetched from tenant-scoped queries (`management/commands/seed_projects.py:326-527`); stdout prints counts and skip-warnings only — no credentials, no secrets, beyond the pre-existing house login pattern which this block does not touch.

---

## Notes from the fixer pass (Phase 5)

All 18 triage items resolved — 18 fixed, 0 skipped — one file per commit, nothing pushed.
Verification: `manage.py check` clean; `makemigrations --check --dry-run` → "No changes
detected" (every fix is code/template/seed/doc level, no migration); `temp/qa73_review.py`
re-run after the M4 reseed → **108 checks, 0 failures**; a fixer render pass
(`temp/fixer73_render_check.py`, 25 checks) re-rendered the touched pages as **admin_acme AND
sales_acme**: Assign/Substitute now appear exactly on the (status × named/placeholder) cells
the views accept — the runtime-confirmed bug (Assign on named soft RAL-00003) is gone —
Approve-Week's confirm carries the resource NUMBER and not the name, Edit/Delete are hidden on
approved/rejected rows and still offered on draft/submitted, `?resource=` pre-selects the
resource on `ral_create`, and the I3 refusal fires with the row unchanged.

Items for later passes (NOT fixed here, out of the 7.3 fixer's scope):

- **Contract seeder-figure drift from M4 (one line, needs the docs pass).** The contract's
  seeder section still pins allocation row 1 as "firm named — resource#1, **16h/wk**" while the
  as-built seed now writes **48h/wk** (a20df48c, per M4). Amending it was outside this pass's
  sanctioned file scope (`contract-projects-7.3.md` — M11 amendments only); Phase 7's docs pass
  should update that one figure (and its "capacities varied so over-allocation is honest"
  rationale now actually fires).
- **qa73_review.py expectations updated in the temp script only** (gitignored, never
  committed): over_count 0→recompute-agreement ≥1, RSP-00001 W37 (16.00, False)→(48.00, True),
  the green-all-clear HTML assert → red-alert assert, and the week-36 RSP-00001 planned
  16.00→48.00 / variance −10.00→−42.00. The contract pins row SHAPES, not the 16h figure, so no
  pinned assert was violated. Note the capacity green all-clear is now — by M4's design — no
  longer reachable from seed data (the demand-side green state was never seeded: gap_count=3);
  the branch logic itself stays covered by the view-vs-recompute agreement.
- **Observation for Phase 6/7 (no lane flagged it):** the capacity board's zero cells render
  the unquantized sentinel too — `{{ cell.planned }}h` prints "0h" for empty resource-weeks
  (`CapacityDemand.py` builds `planned` from a bare sum, no `q2`). Same cosmetic shape as M3
  but on a different view, so it stayed untouched per the no-out-of-scope rule.

Tree hygiene: the only pre-existing untracked files (`.workbuddy-ai/`, `.zcode/`,
`apps/projects/tests/test_planning_forms.py`) are not this pass's — left uncommitted; no 7.4
file (CostBudget*/ProjectBudget*/ProjectExpense/BudgetRevision*, `templates/projects/cost*/`,
migration 0005, 7.4 LIVE_LINKS/seeder hunks) was opened for edit or committed.

