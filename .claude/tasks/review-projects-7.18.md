# Review — Projects 7.18 Integration & API Hub

> Findings file for Phase 4 (six serial reviewers over `c6a4d0da...HEAD`, 7.18 paths only).
> Concurrent 0.15 commits and dirty `templates/projects/reporting/*` working-tree files are out of scope (L45).
> Process: append each agent's findings here immediately; dedupe + assign final C/I/M IDs after all six; commit the file.

---

## Lane 1 — code-reviewer

Scope verified: 4 models, 3 forms (+ deliberate run-form absence), 26 views, 31 url names (all reverse OK), 5 url modules, re-export blocks (4/4 models, 3 form modules, 26 views, 5 url imports), migration `0026` (`makemigrations --check` = no changes, `manage.py check` = clean), LIVE_LINKS keys match `NavERP.md:1290–1294` character-for-character, overview 3 keys + 4 quick-link rows, no outbound HTTP, no `*_advanced.py`, all-absolute imports, tenant scoping on every view, append-only routes (no syr create/edit/delete).

| ID | Severity | File:line | What (the defect) | Why it matters | Suggested fix |
|----|----------|-----------|-------------------|----------------|---------------|
| C1 | Critical | — | — | — | — |
| I1 | Important | `apps/projects/admin.py:934` | `ProjectIntegrationConnectorAdmin.readonly_fields` includes `"credential"`, directly contradicting the class's own comment at `:928-929` ("deliberately absent from every rendering list") and contract §6 ("**never** put `credential` in any `list_display`/`fields`/`readonly_fields` that renders its value"). `readonly_fields` renders the value on the admin change form (the Fernet ciphertext). | Contract violation on the one field the whole credential design protects; the comment now lies to future readers. Ciphertext isn't plaintext, but the contract bans rendering it at all. | Remove `"credential"` from `readonly_fields` (keep it out of `list_display`/`fields` too). |
| I2 | Important | `apps/projects/management/commands/seed_projects.py:4790-4792` | In the 45-run loop, `job.run_count += 1`, `job.last_run_at = started`, `job.last_status = run_status` are mutated in memory but **never saved** (no `job.save()` / `save(update_fields=…)` anywhere after the loop). | All 9 seeded jobs persist `run_count=0`, `last_run_at=None`, `last_status=""` while 45 runs exist. `syj_list` "Runs"/"Last Status" columns, `stats.runs_total` vs per-row counts, connector-detail job table, and `last_status` (contract §2.3: "mirrors the newest run's status for the list column") all disagree with the run log — dead code signals an abandoned intent. | After the run loop, save each job (`ProjectSyncJob.objects.bulk_update(jobs, ["run_count","last_run_at","last_status"])`). |
| I3 | Important | `apps/projects/management/commands/seed_projects.py:4752-4769` | Contract §8 "**≥1 failure per connector**" not met: the 6 `failed` runs land on `jobs[i%9]` for i=30–35 → job indices 3–8 (crm, hris×2, jira, github, custom). The **erp** connectors (jobs 0–1) get zero failures, and **sharepoint** (storage) has no jobs at all → zero runs. | Seeded demo data violates an explicit contract §8 requirement; the storage bullet's connector shows an empty health lens, and the error-connector story (hris) isn't matched by a healthy-then-failing ERP story. | Assign at least one `failed` run per connector (e.g., pin failure indices to cover jobs 0, 1, and a new sharepoint job, or distribute failures by connector rather than straight `i % 9`). |
| M1 | Minor | `seed_projects.py:4726-4735` | No job with `entity_scope="folders"`, and the **sharepoint** connector receives no jobs at all — contract §0 maps the storage bullet to `documents·folders` jobs. | Contract §8 "every `entity_scope` family the five bullets use" is under-covered; storage integration is represented only by a connector row. | Swap or add one of the 9 specs to `(sharepoint, "Folder Tree Sync", "folders", …)` (keep the count at 9). |
| M2 | Minor | `seed_projects.py:4730, 4739` | Job spec uses direction literal `"from_remote_in"` — not in `DIRECTION_CHOICES` — then normalizes it with `direction = "inbound" if direction == "from_remote_in" else direction`. | Invalid choice value in data + a patch-up line: leftover corruption from the lock-race merge; confusing to future editors and easy to regress (if the hack line is deleted, `get_direction_display` shows the raw literal). | Use `"inbound"` in the spec tuple and delete the normalization line. |
| M3 | Minor | `seed_projects.py:4714-4715` | Salesforce/crm `value_map` is `{"Acme Manufacturing": "Acme Manufacturing Ltd."}`; contract §8 pins `{"Closed Won":"active"}` for the Salesforce row (Jira's `{"done":"completed"}` is present). | Contract §8 pins the exact demo maps — drift suggests the seeder was written against something other than the frozen contract. | Use `{"Closed Won": "active"}` (keyed to an appropriate crm local field, e.g. a stage-ish mapping) per §8. |
| M4 | Minor | `apps/projects/admin.py:956-968` | `ProjectSyncRunAdmin` disables add/delete but not change, and `tenant` is not in `readonly_fields` — the only editable field left on the change form is `tenant`, so a staff user can re-parent a run to another workspace on an append-only log. | Append-only invariant (contract §2.4) is weakened at the admin boundary; cross-tenant row moves bypass every view's `tenant=request.tenant` filter. (Shape mirrors 7.17's `ProjectWebhookDeliveryAdmin`, which contract §6 says to mirror.) | Add `def has_change_permission(...): return False` (view-only admin), or add `tenant` to `readonly_fields` + `exclude`. |
| M5 | Minor | `templates/projects/integrationapihub/mapping/list.html:115-118`; `syncjob/list.html:134-137`; `syncrun/list.html:133-136`; `boards/sync_monitor.html:100-103` | Pagination links preserve only a subset of active filters — mapping drops `direction`/`transform`; syncjob drops `entity_scope`/`trigger_mode`/`is_active`; syncrun + monitor drop `trigger_source`/`connector`/`date_from`/`date_to`. | Page 2 silently widens the result set back toward unfiltered (filtered view shows wrong rows after paging); `connector/list.html` gets it right, so the inconsistency is visible. | Carry every active GET param in the Previous/Next querystrings (same pattern as `connector/list.html:151-154`). |
| M6 | Minor | `templates/projects/integrationapihub/connector/list.html:9` | Header uses `{{ domain\|capfirst }}` → renders "Erp Connectors", "Devops Connectors", "Crm Connectors" instead of the `DOMAIN_CHOICES` labels ("ERP & Finance", "DevOps", "CRM"). | Cosmetic contract drift (contract §4: the five category routes echo the domain; labels should match the pinned vocabulary). | Echo the choice label (pass `domain_label` from the view, or loop `domain_choices` to find the matching label). |
| M7 | Minor | `templates/projects/integrationapihub/connector/detail.html:72` | "Field Mappings ({{ mappings\|length }})" shows the **20-row slice** length as if it were the total (`ixc_detail` passes `[:20]`). | A connector with 37 mappings reads "Field Mappings (20)" — understates the register and looks like data loss. | Pass a `mappings_total` count from `ixc_detail`, or label it "Field Mappings (first {{ mappings\|length }})". |
| M8 | Minor | `.claude/tasks/lessons.md` (absent entry) | Contract §0 requires the four-register L36 ruling to land in three places — model docstring ✓ (`Connectors.py:6-15`), `navigation.py` comment ✓ (`:2116-2121`), **`lessons.md` entry ✗** (grep = 0 hits for 7.18 / IntegrationEndpoint / four registers). | The next agent to touch "integration" anything won't see the ruling in the self-improvement file the workflow mandates reading at session start — exactly how a later pass "consolidates" the four registers. | Append the §0 ruling entry (scm CNX / inventory INT / accounting 2.15 / this IXC — four registers, deliberately not merged; no outbound HTTP, no accounting row). |
| M9 | Minor | `apps/projects/views/IntegrationApiHub/Connectors.py:191-194` | `ixc_test` writes no `ProjectSyncRun` when the connector has zero jobs (warning redirect instead); contract §4 states flatly "ixc_test writes a `simulated` `ProjectSyncRun` via `record()`". | Contract-literal gap: smoke/review assertions that POST `ixc_test` and then count runs will fail on a jobless connector; the precondition is undocumented on the verb. | Note the job precondition in the contract/view docstring, or have `ixc_test` handle the jobless case as explicitly documented behavior — the warning path must be an agreed behavior, not an accident. |

**Counts vs contract:** 4 models ✓ · 3 form modules / 4 classes incl. `ConnectorTestForm`, no run form ✓ · 26 views ✓ (9+5+7+3+2) · 31 routes ✓ · seeder guard + after-7.17 call + children-first flush above webhook deletes + 7/30/9/45 via `record()` + summary line ✓ · admin 4 models, credential absent from `list_display` ✓ · LIVE_LINKS 5 bullet keys + 6 extra leaves ✓ · overview 3 keys + 4 rows ✓ · migration↔models ✓.

---

## Lane 2 — explorer (structural / plan-vs-as-built / sibling-seam)

Scope: `c6a4d0da...HEAD`, 7.18 paths only; new findings (no overlap with Lane 1's I1–I3 / M1–M9); contract `contract-projects-7.18.md`, plan `todo.md:9389+`.

| ID | Severity | File:line | What (the defect) | Why it matters | Suggested fix |
|----|----------|-----------|-------------------|----------------|---------------|
| E1 | Important | `apps/projects/forms/__init__.py:210–216` (commit `10dff912`); `apps/projects/views/__init__.py:734–770` (commit `c7645b2e`) | The 7.18 re-export blocks were committed **inside the 7.16 lane's commits** — `git show 10dff912` adds both the 7.16 forms block *and* the whole 7.18 forms block; `-S 'IntegrationApiHub.Connectors import'` attributes both files to `10dff912`/`c7645b2e` ("re-export … Reporting & BI … (7.16)"; `c7645b2e` body claims "nothing else is touched"). Neither commit is in `c6a4d0da...HEAD` (both ancestors of BASE); the contract's own base `9336e994` does include them (158 commits in `9336e994..c6a4d0da`). | (a) The 7.18 shared-file seam hunks are invisible to this review range; (b) both commits precede the entity modules — `git ls-tree c6a4d0da apps/projects/{forms,views,models}/IntegrationApiHub/` = empty — so history from `10dff912`/`c7645b2e` until `fe7bcfd3`/`ed9d0da7`/`9700762c` has `ImportError` on `import apps.projects.forms/views` (wiring-before-files violates Integrate's "verify files landed first"; L43 two-sessions-one-checkout `git add` sweep). HEAD content is correct. | No HEAD code change. Orchestrator: extend seam review of these two files over `9336e994..c6a4d0da`, or record the provenance here; fixer marks N/A-process. |
| E2 | Important | `views/IntegrationApiHub/Connectors.py:186–206` (+ `forms/IntegrationApiHub/Connectors.py:74–80`, `connector/detail.html:147–149`) | `ixc_test` never binds `ConnectorTestForm` — the posted `note` (rendered at detail:149, placeholder "Optional note recorded on the simulated test run") is silently dropped; `record()` gets a fixed `error_message` only. | Plan pins it: `todo.md:9438` — "an optional note … the `ixc_test` verb **reads** when composing the `simulated` run's `payload_excerpt`". Contract §4 is silent, so the item was lost without an amendment; the UI promises recording that never happens. (Distinct from M9's jobless-connector path.) | In `ixc_test`, bind `ConnectorTestForm(request.POST)` and pass the note into `ProjectSyncRun.record(payload_excerpt=…)` (truncated); amend contract §3.1/§4 to pin note→`payload_excerpt`. |
| E3 | Minor | `views/IntegrationApiHub/Connectors.py:84` (`"due_rotation": 0`); contract §4:316 | `stats.due_rotation` hard-coded `0` and no template reads it (`connector/list.html:40–44` renders 5 other cards) — dead context key. | `todo.md:9448` said "pin the rotation window for `due_rotation` in the contract"; contract pinned the key but never defined computation — unfinished plan item, not just dead code. | Contract amendment: define the window (e.g. connected + stale `last_success_at`, or `consecutive_failures>0`) and compute it — or drop key from contract + view. |
| E4 | Minor | `models/IntegrationApiHub/Connectors.py:151–156` | No `clean_name` / `project IS NULL` duplicate guard: `unique_together=[("tenant","project","name")]` cannot bind NULL projects, so two workspace-wide connectors may share a name. | `todo.md:9425` and `:9437` both required the "`project IS NULL` name-duplicate check the DB constraint cannot express (research §6.3)"; contract §2.1/§3.1 froze only the tenant guard and dropped this silently. | Contract amendment: adopt (`clean_name` → `ValidationError` on sibling `(tenant, project=NULL, name)`) or explicitly defer with rationale. |
| E5 | Minor (process) | commits `dac0ec42` (3 files: migration 0026 + `models/__init__.py` + `urls/…/HubBoards.py`), `cb9366b0` (3 templates), `18202358` (2 board templates) | Three in-range commits bundle multiple files, each message describing only a subset (`dac0ec42` = "migration 0026…" while also landing the models re-export block and a url module). | Violates strict one-file-per-commit; the `models/__init__.py` wiring has no dedicated commit/message, so history search by message won't find it. | Cannot unbundle without rewrite — record as known history; fixer N/A; apply rule going forward. |
| E6 | Minor (process) | commits `73781f0b`, `a2b5e241`, `b16495ff` | Message ≠ content: `73781f0b` "empty IntegrationApiHub views sub-package" actually adds `forms/IntegrationApiHub/SyncJobs.py`; `a2b5e241` "connector routes…" actually adds `urls/IntegrationApiHub/__init__.py`; `b16495ff` "overview 7.18 stat cards…" actually adds the `navigation.py` LIVE_LINKS block — each duplicating a sibling commit's correct message (`91349e89`, `d1734038`, `462ea7e2`). | Rule requires each message specific to its one file; duplicates make `git log --grep` attribution wrong (and hid E1-style smuggling). | Record only (history immutable without rewrite). |

**Contract amendments needed (L2/Q9):** E2 (pin note routing), E3 (`due_rotation` semantics), E4 (NULL-name guard adopt/defer), E1 (note that forms/views wiring lives in `9336e994..c6a4d0da` under 7.16-labeled commits — reviewers using the contract base see it; reviewers using `c6a4d0da` do not).

**Clean areas (L2 evidence):** plan↔contract §3–§8 fully landed (models/forms/views/urls/admin/seeder/navigation/overview/migration 0026/settings untouched; remaining plan gaps all owned by Lane 1) · every in-range shared-file diff additive, no 7.16/7.17 block reordered, 0.15 commits touch only `apps/core`, dirty `reporting/*` untouched (L45) · URLconf literal-first, zero `<str:`/`<path:` converters, `integration/` disjoint, app mounted at `projects/` · backend 1:1 with contract file list, absolute imports · SyncRun append-only (no form module/template, routes list/detail/retry, admin add/delete off) · no outbound HTTP in 7.18 paths · `IXC`/`SYJ`/`SYR` prefixes unique · seeder helper methods still single-defined after the `_integration_hub` insert.

---

## Lane 3 — frontend-reviewer

Scope: the 14 new `templates/projects/integrationapihub/**` + 7.18 hunks in `templates/projects/overview.html` over `c6a4d0da...HEAD`. Full 12-point checklist run (comment leak, theme classes, design system, CRUD, badges, pagination guards, None-safety, URLs, pk filters, responsive/dark/RTL, a11y, HTMX, structure).

| ID | Severity | File:line | What (the defect) | Why it matters | Suggested fix |
|----|----------|-----------|-------------------|----------------|---------------|
| F1 | Minor | `connector/detail.html:155–169`; `mapping/detail.html:52–58`; `syncjob/detail.html:84–95` | The "Actions" sidebar carries Edit/Back (and toggle/rotate) but **not** the POST-Delete button — Delete lives only in the page-header `page-actions` (connector:29, mapping:22, syncjob:29). Checklist #4 / CLAUDE CRUD rule requires Edit + POST-Delete + Back in the actions sidebar. (7.17's `webhook/detail.html` also puts Delete in the header, so sibling precedent is split.) | Rule-literal gap: the sidebar is the documented home for destructive actions; a reader scanning Actions finds Delete missing and may assume the entity can't be deleted. | Move (or duplicate) each Delete form — `{% csrf_token %}` + `onsubmit="return confirm(...)"` + `btn btn-danger` — into the Actions `card-body` alongside Edit/Back, mirroring the header button. |
| F2 | Minor | `connector/detail.html:149` | `{{ test_form.note }}` renders the textarea with no `<label>` (widget has only a placeholder); every other input in the module has `label for=…`. | A11y checklist #11: placeholder is not a label; screen readers announce an unlabeled edit field. | Add `<label for="id_note" class="form-label">Note (optional)</label>` above the field (Django renders the textarea `id="id_note"`). |
| F3 | Minor | `boards/integration_hub.html:61` | Health indicator renders an **empty** badge — `<span class="badge {{ c.health_badge }}">&nbsp;</span>` — with no text, `title`, or `aria-label`. | A colored pill with no accessible name conveys nothing to screen readers and is ambiguous visually (is it a dot? a status?); checklist #11. | Add `title`/`aria-label` from a health verb (e.g. `{% if c.consecutive_failures >= 3 %}title="Failing"{% elif c.consecutive_failures %}title="Degraded"{% else %}title="Healthy"{% endif %}`) or render the label text. |

**Checked clean (L3 evidence):** zero multi-line `{# #}` comment leaks (0 hits) · every `badge-*`/`stat-icon <x>` used exists in `theme.css` (green/red/amber/info/muted/slate; blue/green/orange/purple/slate/**red** — `.stat-icon.red` is in theme.css:265, and `status_badge`/`domain_badge`/`health_badge`/`status_badge` (run) all `.get(..., "badge-slate")`-fallback ✓) · all 14 pages `{% extends "base.html" %}`, `.page-header/.card/.table-wrap/.form-*/.stat-card/.empty-state/.pagination/.btn-outline/.form-error` all defined in theme.css (`card-footer`/`inline` are absent from theme.css but are the codebase-wide convention used identically by 7.15/7.17 — not a 7.18 regression) · list pages all have GET filter forms with labeled controls, `request.GET`/context re-selection, and `|stringformat:"d"` for pk options (no `|slugify` anywhere) · Actions columns eye/pencil/trash with POST+csrf+confirm ✓, `colspan` matches header counts (8/8/9/13/5/4/8) · every `page_obj.previous/next_page_number` guarded by `has_previous`/`has_next` ✓ (filter-param drops are Lane 1's M5) · None-safe FKs wrapped in `{% if %}` (owner, project, triggered_by, last_sync_at, finished_at, next_retry_at) · all `{% url %}` names in the 31-route set · credential never rendered raw (`credential_set`/`credential_masked` + session-once `revealed_credential` with amber one-time-reveal banner) · no `dark:` variants anywhere in `templates/projects/` and `mr-1`/RTL patterns match siblings — codebase-wide, not this lane's regression · no HTMX on these pages (full-page POSTs; lucide via base) · folder shape `integrationapihub/<entity>/<page>.html` + boards at sub-module root ✓ (no flat `<entity>_*.html`).

**Praise (L3):** the connector detail page is a model of honest UI — every "integration" affordance is labeled as simulated/recorded-not-executed (`Run Test (Simulated)`, "nothing runs automatically" notes on schedule fields, "Recorded, never evaluated" on the filter expression), so the zero-outbound-HTTP contract is legible to the user, not just to the reviewer.

---

## Lane 4 — performance-reviewer

Scope: `apps/projects/views/IntegrationApiHub/*` + 7.18 hunk of `views/ProjectInitiation/Overview.py` + `models/IntegrationApiHub/*` Meta + seeder run loop, over `c6a4d0da...HEAD`.

| ID | Severity | File:line | What (the defect) | Why it matters | Suggested fix |
|----|----------|-----------|-------------------|----------------|---------------|
| P1 | Important | `views/IntegrationApiHub/Connectors.py:100–102` → `connector/detail.html:130` | `recent_runs = ProjectSyncRun.objects.filter(...).order_by("-started_at")[:15]` has **no `select_related("job")`**, but the template renders `{{ r.job.number }}` on every row. | Classic N+1 on a hot detail page: 1 + up to **15 extra queries** per connector-detail view (one per row's job FK). Every other run-rendering view in the module already joins (`syr_list:57`, `syr_detail:97`, `integration_hub:49`, `sync_monitor:77` all use `select_related("job", ...)` — this one was missed). | `.filter(tenant=request.tenant, job__connector=connector).select_related("job").order_by("-started_at")[:15]`. Recommend `django_assert_max_num_queries` on `ixc_detail` in the test lane. |
| P2 | Important | `views/IntegrationApiHub/HubBoards.py:20–38` | The hub landing runs **~17 connector queries where 1 would do**: `conn_counts` aggregate (1) + `by_status` GROUP BY (1) + `by_domain` GROUP BY (1) + a `for value, label in DOMAIN_CHOICES` loop issuing `dom.count()` + `connected.count()` + `error.count()` per domain = **15 queries** (lines 30–38). `by_status`/`by_domain` are contract-pinned (contract §4:359) but **never rendered** by `integration_hub.html`; the loop recomputes overlapping numbers again. | Dashboards are a named hot path (checklist #5: multiple KPIs over one table must share a single aggregate, not one query per stat card). 15 sequential COUNTs on every hub page view — grows with domain count, not table size. | One cross-tab: `connectors.values("domain", "status", "is_active").annotate(c=Count("id"))` (single GROUP BY) → derive `domains` (total/connected/failing per domain), `stats.by_domain`, `stats.by_status`, and the `active` count in Python; keep `conn_counts` only for the fields the pivot can't supply, or fold `is_active` into the pivot and delete lines 20–38's redundant queries. |
| P3 | Minor | `views/IntegrationApiHub/Connectors.py:228–235` | `connector_health` issues **3 sequential counts** on the same `runs` queryset (`runs.count()`, `runs.filter(status="failed").count()`, `runs.filter(status="success").count()`); additionally `jobs`/`mappings` querysets (228–229) are passed solely for `{{ jobs\|length }}` / `{{ mappings\|length }}` (connector_health.html:40–41), so `|length` fetches **every row** to count it (checklist #3: `qs.count()`, not `len(qs)`). | 4 queries + full-row fetches where 3 COUNTs suffice; small today, wrong shape for a board meant to sit on a large run log. | `runs.aggregate(total=Count("id"), failed=Count("id", filter=Q(status="failed")), success=Count("id", filter=Q(status="success")))` (1 query); pass `jobs_count=jobs.count()`, `mappings_count=mappings.count()` (or keep querysets but compute counts in the view). |
| P4 | Minor | `views/IntegrationApiHub/SyncRuns.py:69`; `views/IntegrationApiHub/HubBoards.py:88`; `views/ProjectInitiation/Overview.py:173–181` | `failed_today` is issued as a **second** query after the status aggregate on the same table (syr_list, sync_monitor); the overview does the same pattern — `sync_runs_today` and `sync_runs_failed_today` are two separate `.count()`s (Overview.py:175, 179) on `ProjectSyncRun`. | Each site is one avoidable COUNT; overview runs on every dashboard hit. | Fold into the aggregate already being computed: `failed_today=Count("id", filter=Q(status="failed", started_at__gte=today_start))` inside the same `.aggregate(...)`; in Overview, one `.aggregate(total=Count("id", filter=Q(started_at__gte=today)), failed=Count("id", filter=Q(status="failed", started_at__gte=today)))`. |
| P5 | Minor | `models/IntegrationApiHub/SyncJobs.py:71–74` | `ProjectSyncJob.Meta.indexes` has `(tenant, connector)`, `(tenant, connector, is_active)`, `(tenant, last_status)` but **no `(tenant, is_active)`**, while sibling `ProjectIntegrationConnector` ships exactly that (`ixc_tnt_active_idx`, Connectors.py:145). `syj_list` filters `is_active` alone on its State dropdown (SyncJobs.py:28–32) — the hot tenant-scoped filter has no covering index. | Checklist #6: tenant-scoped hot filter should be indexed; the module already established the pattern on the sibling table, so this is an intra-module inconsistency, not an app-wide pass. | Add `models.Index(fields=["tenant", "is_active"], name="syj_tnt_active_idx")` to `ProjectSyncJob.Meta.indexes` (follows via `makemigrations`). |

**Checked clean (L4 evidence):** every other list/detail joins its rendered FKs — `ixc_list` (`project`, `owner`, `notify_webhook`), `syj_list`/`syj_detail`/`syj_run` (`connector`), `ixm_list`/`ixm_detail` (`connector`), `syr_list`/`syr_detail` (`job`, `job__connector`[, `triggered_by`]), hub/monitor runs (`job`, `job__connector`) ✓ · no chained `__str__` hops (`Connector.__str__`/`SyncRun.__str__` touch only local fields + `get_*_display`) ✓ · filters applied **before** `Paginator` everywhere; page size 25 matches 7.17 siblings (`WorkflowRules.py:52` etc.) ✓ · KPIs all `aggregate`/`Count`-derived, never Python loops over rows (the one Python loop is P2's count fan-out) ✓ · indexes cover the hot tenant filters: `ixc` (tenant,domain/status/project/is_active), `syr` (tenant,job,status)/(tenant,status,started_at)/(tenant,job,started_at), `ixm` (tenant,connector[,is_key]) ✓ · no `len(qs)`/`if qs:` misuse in views, no template-loop queries, no `.all()` in loops ✓ · seeder's per-row `record()`/`.save()` is contract-mandated (`TenantNumbered` number allocation bypasses `bulk_create` — documented at seed:1724/1840) ✓ · unused lazy context keys (`syr_list.connectors`, hub `projects`) are contract-pinned and never evaluated → 0 queries ✓.

---

## Lane 5 — qa-smoke-tester (runtime verification, report-only)

Environment: `venv\Scripts\python.exe` · `migrate` = no changes · `seed_core`/`seed_accounts`/`seed_projects` all idempotent-skip with **"7.18 integration hub already seeded"** for both Acme and Globex ✓. Throwaway script `temp/smoke_718_review.py` (in-process test client, `raise_request_exception=False`, logged in as `admin_acme`).

**Result: TOTAL=55 PASS=55 FAIL=0 — no fixes needed.**

| Group | URLs | Result |
|-------|------|--------|
| Module landing + boards (content-asserted) | `overview` (asserts Integration Hub/Sync Monitor/Connectors/Sync Jobs rows), `integration_hub` (asserts a real connector number + Failed Today), `sync_monitor` (asserts a real run number) | 3× 200 + tokens ✓ |
| Connector GETs | `ixc_list` + 5 domain lists + `ixc_create`/`ixc_detail`(number+name+test card)/`ixc_edit`/`connector_health` | 10× 200 + tokens ✓ |
| Mapping GETs | `ixm_list`/`ixm_create`/`ixm_detail`(local+remote field)/`ixm_edit` | 4× 200 + tokens ✓ |
| Job GETs | `syj_list`/`syj_create`/`syj_detail`(number+name)/`syj_edit` | 4× 200 + tokens ✓ |
| Run GETs | `syr_list`/`syr_detail`(number) | 2× 200 + tokens ✓ |
| POST-only verbs via GET | `ixc_delete`, `ixc_rotate_credential`, `ixc_test`, `ixc_toggle_active`, `ixm_delete`, `syj_delete`, `syj_toggle_active`, `syj_run`, `syr_retry` | 9× **405** ✓ (method guards live) |
| Filtered / junk-param / page-2 | `ixc_list?q&status&is_active`; `ixm_list?category=abc&foo&direction=nonsense`; `syr_list?status=zzz&date_from=not-a-date&job=abc`; `ixm_list?page=2` (30 rows > 25); `syr_list?page=2` (45 rows); `syj_list?page=2`; `sync_monitor?status=failed&page=2` | 7× 200, no 500 ✓ (L9/L11) |
| Cross-tenant IDOR — GET | globex pks on `ixc_detail`/`ixc_edit`/`connector_health`/`ixm_detail`/`ixm_edit`/`syj_detail`/`syj_edit`/`syr_detail` | 8× **404** ✓ |
| Cross-tenant IDOR — POST | globex pks on `ixc_delete`/`ixc_test`/`ixc_toggle_active`/`ixm_delete`/`syj_delete`/`syj_run`/`syr_retry`/`ixc_rotate_credential` | 8× **404** ✓ (guards before any write; DB untouched) |

Content checks also assert **no `{#` and no unparsed `{% comment` in any rendered 200 body** (comment-leak class) and a real seeded identifier token on every detail page (silent-blank-context class, L8). Every group passed. (The `Method Not Allowed`/`Not Found` lines in the log are Django's request logging of the *expected* 405/404 assertions above.)

No acme-side mutating POSTs were replayed in this review pass — the Phase-3 build smoke already exercised them (`temp/smoke_718.py`, 76/0/0, since deleted per the skill), and the reviewer's charter is report-only; mutating acme verbs would alter seeded demo state.

---

## Lane 6 — security-reviewer

Scope: all 7.18 paths under `apps/projects/{models,forms,views,urls}/IntegrationApiHub/`, the 14 `templates/projects/integrationapihub/**` templates, `admin.py:926–968`, `seed_projects.py:_integration_hub`, and the related hunks in `config/settings.py` / `views/ProjectInitiation/Overview.py`, over `c6a4d0da...HEAD`. Read the security-reviewer checklist and swept every category.

**Result: 0 Critical / 0 Important / 0 Minor findings. Clean pass.**

| Check | Evidence |
|-------|----------|
| **Tenant scoping / IDOR** | Every queryset filters `tenant=request.tenant` (47 hits across the 5 view modules); every `get_object_or_404(..., pk, tenant=request.tenant)` (17 sites). Runtime: 8 cross-tenant GET→404 + 8 cross-tenant POST→404 (Lane 5). Forms use `TenantModelForm` + `_reject_foreign` on every FK (`project`, `notify_webhook`, `connector`) + `TenantUniqueMixin`. `owner` explicitly narrowed to `User.objects.filter(tenant=self.tenant)` (Connectors.py:54–56). |
| **AuthZ / decorators** | All 26 views `@login_required`. Admin-gated (contract § lines 230–232/324/356): `ixc_delete`, `ixc_rotate_credential`, `ixm_delete`, `syj_delete`, `syr_retry` = `@require_POST + @tenant_admin_required`. Contract-pinned as `@require_POST` only: `ixc_test`, `ixc_toggle_active`, `syj_run`, `syj_toggle_active` — matches as-built exactly. No `@csrf_exempt`, no `permission_required` gaps. |
| **CSRF** | 19 POST forms across the 14 templates; every one carries `{% csrf_token %}` (verified per-file: connector 7, mapping 3, syncjob 7, syncrun 2). `CsrfViewMiddleware` in settings; `CSRF_COOKIE_SAMESITE="Lax"`, `CSRF_COOKIE_SECURE` when `DEBUG=False`. All delete/rotate forms use `method="post"` + `onsubmit="return confirm(...)"`. |
| **XSS / escaping** | Zero `|safe`, zero `{% autoescape off %}`, zero `mark_safe` in 7.18 views/forms/templates. All reflected values (`{{ q }}`, `{{ status }}`, `value="{{ … }}"`) autoescaped; `confirm()` interpolations use only system numbers (`IXC-`/`SYJ-`/`SYR-`), never user text (L42 clean). |
| **SQL injection** | Zero `.raw()`, `.extra()`, `RawSQL`, `cursor.execute`, f-string SQL. All filters via ORM `Q()` / `__icontains` / equality. `datetime.strptime` dates wrapped in try/except `ValueError`. |
| **Credential handling** | Fernet via `apps.core.crypto` (key from `FIELD_ENCRYPTION_KEY`/`SECRET_KEY`, never in DB). `credential` is `editable=False` + write-only extra form field (`PasswordInput(render_value=False)`) + `credential_set`/`credential_masked` (last-4 only) in templates. Session-once reveal: `request.session["_ixc_cred_reveal"]` set on rotate (Connectors.py:180), `session.pop` on detail (105–107), pk-matched so a stolen session token only reveals the intended connector. `SESSION_COOKIE_HTTPONLY=True`, `SameSite=Lax`, 12h age; full `SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE`/HSTS/nosniff/proxy-SSL block when `DEBUG=False` (settings.py:172–180). `write_audit_log` on rotate records no secret value. **Admin `readonly_fields` exposing ciphertext is already I1** — not re-reported. |
| **Secrets in source / seeder** | `_integration_hub` seeds demo tokens via `set_credential(...)` → Fernet-encrypted at rest (`seed_projects.py:4600/4614/4637`); plaintext literals exist only in the seeder source (demo-data convention shared by every module seeder). No secrets in templates, messages, or audit `changes` payloads. |
| **Open redirect** | All 16 `redirect()` targets are hardcoded `projects:<name>` url names — no `request.GET["next"]`, no user-supplied URL. |
| **Mass assignment** | 3 ModelForms pin explicit `Meta.fields` (17/11/…); `tenant`, `number`, `status`, `credential` (model col), `last_*`, `consecutive_failures` all off the form or `editable=False`. `ConnectorTestForm` is a non-model form with one optional field. `_reject_foreign` blocks crafted FK pks. |
| **SSRF / outbound HTTP** | Zero `requests`/`urllib`/`httpx`/`socket`/`urlopen` in the package. `base_url` carries a model-level `WARNING` (Connectors.py:103–105): CharField, config-only, never fetched — matches contract §0 "performs no outbound HTTP". `ixc_test`/`syj_run` record `simulated` runs only. |
| **Audit trail** | `write_audit_log` on every mutating verb (create/update/delete/rotate/test/toggle/run/retry); delete rows store number-only changes. |
| **Session fixation / hardening** | `SESSION_IDLE_TIMEOUT` 30m + `SessionTimeoutMiddleware`; `X_FRAME_OPTIONS="DENY"`; `SECRET_KEY` required from `.env` when `DEBUG=False` (fails hard); `ALLOWED_HOSTS` env-driven; password validators enabled. |
| **File upload / deserialization** | None in 7.18 (no `FileField`, no `pickle`/`yaml.load`/`eval`/`exec`). `value_map` JSONField parsed via `json.loads` in form `clean_value_map` only. |

**Lane 6 notes (not findings):** (1) `base_url` is intentionally unvalidated as a URL — the model documents this as config-only with zero fetches; if a future sub-module ever fetches it, re-open as SSRF with a scheme/host allowlist. (2) Seeder demo tokens are plaintext in source but encrypted before insert — same convention as every other NavERP seeder. (3) `ixc_test`/`syj_run` being available to non-admin logged-in users is contract-pinned (read-only simulated write, tenant-scoped); not a privilege gap.

---

## Final deduped findings (post-lane-6 close-out)

All six lanes complete: code-reviewer → explorer → frontend → performance → qa-smoke (55/55, no findings) → security (clean pass). Deduped: Lane 2 explicitly confirmed no overlap with Lane 1; E2 (note dropped) and M9 (jobless path) are distinct defects on the same verb. **0 Critical. 7 Important. 19 Minor.** Lane-1 `C1` row was an empty placeholder (no criticals found by any lane). Sorted Critical → Important → Minor; IDs reassigned below — **the fixer must use these final IDs, not the per-lane prefixes (I1/I2/…, E1/E2/…, F…, P…)**; the per-lane tables above remain as provenance.

| Final ID | Was | Severity | File:line | What (the defect) | Suggested fix / disposition |
|----------|-----|----------|-----------|-------------------|------------------------------|
| **I1** | L1-I1 | Important | `apps/projects/admin.py:934` | `readonly_fields` includes `"credential"` — renders Fernet ciphertext; contradicts `:928-929` comment and contract §6. | Remove `"credential"` from `readonly_fields`. |
| **I2** | L1-I2 | Important | `seed_projects.py:4790-4792` | Run-loop mutates `run_count`/`last_run_at`/`last_status` in memory but never saves them — all 9 jobs persist `0`/`None`/`""` while 45 runs exist. | `bulk_update(jobs, ["run_count","last_run_at","last_status"])` after the loop. |
| **I3** | L1-I3 | Important | `seed_projects.py:4752-4769` | Contract §8 "≥1 failure per connector" not met: erp (jobs 0–1) get zero failures; sharepoint has no jobs at all. | Distribute the 6 failed runs to cover every connector (incl. erp + a sharepoint job). |
| **I4** | L2-E1 | Important (process) | `forms/__init__.py:210-216` / `views/__init__.py:734-770` (commits `10dff912`/`c7645b2e`) | 7.18 re-export blocks smuggled into 7.16-labeled pre-BASE commits; wiring-before-files broke `import apps.projects.forms/views` from `10dff912` until entity modules landed. HEAD content correct; invisible to `c6a4d0da...HEAD`. | **No HEAD code change.** Orchestrator: extend seam review over `9336e994..c6a4d0da` or record provenance; fixer `[~] skipped — process/history`. |
| **I5** | L2-E2 | Important | `views/IntegrationApiHub/Connectors.py:186-206` | `ixc_test` never binds `ConnectorTestForm` — posted `note` (rendered at detail:149) silently dropped; `todo.md:9438` pinned it as read into `payload_excerpt`. | Bind the form; pass note into `record(payload_excerpt=…)`; amend contract §3.1/§4. |
| **I6** | L4-P1 | Important | `views/IntegrationApiHub/Connectors.py:100-102` → `connector/detail.html:130` | `recent_runs` missing `select_related("job")` while template renders `r.job.number` — 1+N, up to 15 extra queries; every sibling run-rendering view already joins. | `.select_related("job")` on the queryset; assert with `django_assert_max_num_queries` in Phase 6. |
| **I7** | L4-P2 | Important | `views/IntegrationApiHub/HubBoards.py:20-38` | Hub landing issues ~17 queries (15 from a per-domain count loop + overlapping aggregates) where one cross-tab GROUP BY suffices; `by_status`/`by_domain` pinned but never rendered. | Single `values("domain","status","is_active").annotate(c=Count("id"))` pivot; derive all stats in Python. |
| **M1** | L1-M1 | Minor | `seed_projects.py:4726-4735` | No `entity_scope="folders"` job; sharepoint connector gets zero jobs — contract §8 under-covers the storage bullet. | Swap one of the 9 specs to `(sharepoint, …, "folders", …)`. |
| **M2** | L1-M2 | Minor | `seed_projects.py:4730,4739` | Spec uses invalid direction literal `"from_remote_in"` then normalizes it with a patch-up line. | Use `"inbound"`; delete the normalization line. |
| **M3** | L1-M3 | Minor | `seed_projects.py:4714-4715` | Salesforce `value_map` drifted from contract §8's pinned `{"Closed Won":"active"}`. | Restore the pinned map. |
| **M4** | L1-M4 | Minor | `admin.py:956-968` | `ProjectSyncRunAdmin` disables add/delete but not change; only editable field on an append-only log is `tenant` (cross-tenant re-parent). | `has_change_permission → False` (or tenant to `readonly_fields` + `exclude`). |
| **M5** | L1-M5 | Minor | `mapping/list.html:115-118`; `syncjob/list.html:134-137`; `syncrun/list.html:133-136`; `boards/sync_monitor.html:100-103` | Pagination drops active filter params (page 2 widens back to unfiltered); `connector/list.html` gets it right. | Carry every active GET param (same pattern as connector/list:151-154). |
| **M6** | L1-M6 | Minor | `connector/list.html:9` | `{{ domain\|capfirst }}` renders "Erp Connectors" etc. instead of `DOMAIN_CHOICES` labels. | Pass/echo the choice label from the view. |
| **M7** | L1-M7 | Minor | `connector/detail.html:72` | `mappings\|length` shows the 20-row slice as the total. | Pass `mappings_total` (or label "first N"). |
| **M8** | L1-M8 | Minor | `.claude/tasks/lessons.md` | Contract §0's four-register L36 ruling never landed in lessons.md (0 hits). | Append the ruling entry (scm CNX / inventory INT / accounting 2.15 / this IXC — four registers, not merged). |
| **M9** | L1-M9 | Minor | `views/IntegrationApiHub/Connectors.py:191-194` | `ixc_test` writes no run when the connector has no jobs; contract §4 states flatly that it writes a `simulated` run. | Document the precondition in contract/view docstring (distinct from I5's dropped note). |
| **M10** | L2-E3 | Minor | `views/IntegrationApiHub/Connectors.py:84` + contract §4:316 | `due_rotation` hard-coded `0`, never rendered; `todo.md:9448` asked to pin the window but contract never defined it. | Contract amendment: define computation + implement, or drop key from contract + view. |
| **M11** | L2-E4 | Minor | `models/IntegrationApiHub/Connectors.py:151-156` | No `project IS NULL` name-duplicate guard (`unique_together` can't bind NULL); `todo.md:9425/:9437` required it, contract dropped it silently. | Contract amendment: adopt `clean_name` ValidationError or explicitly defer. |
| **M12** | L2-E5 | Minor (process) | commits `dac0ec42`, `cb9366b0`, `18202358` | Three in-range commits bundle multiple files (violates one-file-per-commit). | History immutable — fixer `[~] skipped — process`. |
| **M13** | L2-E6 | Minor (process) | commits `73781f0b`, `a2b5e241`, `b16495ff` | Message ≠ content (misattributed by `git log --grep`). | History immutable — fixer `[~] skipped — process`. |
| **M14** | L3-F1 | Minor | `connector/detail.html:155-169`; `mapping/detail.html:52-58`; `syncjob/detail.html:84-95` | Actions sidebar lacks the POST-Delete button (Delete only in page-header) — CRUD rule #3. | Move/duplicate each Delete form into the Actions sidebar. |
| **M15** | L3-F2 | Minor | `connector/detail.html:149` | `{{ test_form.note }}` textarea has no `<label>` (placeholder only) — a11y #11. | Add `<label for="id_note">Note (optional)</label>`. |
| **M16** | L3-F3 | Minor | `boards/integration_hub.html:61` | Empty `&nbsp;` health badge with no text/`title`/`aria-label`. | Add `title`/`aria-label` from a health verb (or render label text). |
| **M17** | L4-P3 | Minor | `views/IntegrationApiHub/Connectors.py:228-235` | `connector_health` = 3 sequential counts on the same `runs` qs; `jobs`/`mappings` passed only for `\|length` (full fetch). | One `aggregate(...)` with `Count(filter=…)`; pass `.count()` values. |
| **M18** | L4-P4 | Minor | `SyncRuns.py:69`; `HubBoards.py:88`; `ProjectInitiation/Overview.py:173-181` | `failed_today` issued as a second COUNT after the status aggregate; overview does the same twin-count pattern on every dashboard hit. | Fold into the existing aggregate via `Count("id", filter=Q(...))`. |
| **M19** | L4-P5 | Minor | `models/IntegrationApiHub/SyncJobs.py:71-74` | `ProjectSyncJob` missing `(tenant, is_active)` index; sibling connector ships exactly that, and `syj_list` filters `is_active` alone. | Add `models.Index(fields=["tenant","is_active"], name="syj_tnt_active_idx")` + `makemigrations`. |

**Counts:** 0 Critical · 7 Important · 19 Minor · 2 clean lanes (qa-smoke 55/55, security clean). Process-only: I4, M12, M13 (fixer `[~] skipped — reason`).

**Contract amendments for the orchestrator/fixer handoff:** I5 (pin `note` → `payload_excerpt`), M10 (`due_rotation` semantics), M11 (NULL-name guard adopt/defer), I4 (note that forms/views wiring lives in `9336e994..c6a4d0da` under 7.16-labeled commits).

**Phase 4 status: COMPLETE.** Ready for Phase 5 (`code-fixer` agent over this file, final IDs above, one commit per file, `[x]`/`[~]` markers in-place).

---
